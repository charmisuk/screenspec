# 회고 — 2026-08-24 사이클 (v0.14.0)

## 통계
- 이슈 13건 중 10건 처리(커밋 10) · 3건 사용자 판단 대기(#16 #10 #8)
- G3→G2 되돌림 3회: #13(헤더 겹침 미검출), #18(검사식 오류), #9(조상 화면 행 누락) — 모두 1회 만에 해결
- 서브에이전트(opus) 2회: #12+#7 91k 토큰, #14 55k. 나머지 8건은 메인 직접(각 2~40줄)
- e2e 49 → 68

## 분류
| 구분 | 내용 | 제안 |
|---|---|---|
| ➕ Add | 위치·레이아웃 이슈의 accept 에 "다른 fixed UI(헤더·패널)와 겹치지 않음" 이 빠져 실측에서야 발견(#13) | G1 plan 템플릿에 레이아웃 항목 기본 체크 추가 |
| ➕ Add | 예제 수정을 맡긴 서브에이전트가 패널 위치를 스스로 판단(#12) | 예제 수정이 있으면 plan 에 UI 배치 제약 명시 |
| 🔧 Fix | 프로토콜은 dev=서브에이전트 고정이었으나, 10줄 내외 수정은 서브에이전트 컨텍스트 재독 비용(50~90k)이 작업보다 큼 | **dev 라우팅 기준 추가: 예상 변경 ≤40줄·파일 1~2개 → 메인 직접, 그 이상 또는 UI/CSS 다중 지점 → opus** |
| 🔧 Fix | tasks.json 갱신을 CLI 인자로 하다 zsh 글롭(`[`)에 걸림 | 헬퍼가 파일 패치(@file)만 받도록 — 이미 적용 |
| ➖ Remove | 없음 (모든 게이트가 최소 1회 실질적으로 잡아냄: qa 3회 되돌림) | — |
| 💡 Insight | 감지(detectScreen)가 DOM display 를 쓰면 MutationObserver 가 재발화 — 감지는 읽기만 | learnings.md |
| 💡 Insight | getComputedStyle 커스텀 프로퍼티는 var() 치환값 반환 | learnings.md |
| 💡 Insight | 목차 클릭 시 정의서 render 가 앱 라우트 렌더보다 먼저라 누락 경고 false-positive(main 에서도 동일) | 신규 이슈 후보 |

## 프로토콜 반영 제안 (승인 시 issue-cycle.md v1.1)
1. G2 모델 라우팅 표에 "≤40줄·1~2파일 = 메인 직접" 행 추가
2. G1 plan.accept 기본 체크: (a) 다른 fixed UI 와 겹치지 않음 (b) 예제 수정 시 배치 제약
3. Phase 2 전체 QA 스크립트(scratch cycleqa.js)를 `tests/smoke.js` 로 저장소에 내장할지 검토 — 예제 전수 클릭·에러 0 확인

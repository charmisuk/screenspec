{
  "cycle": "2026-08-24-frame",
  "branch": "feat/frame-mode",
  "base_version": "0.14.1",
  "protocol": ".agents/workflows/issue-cycle.md",
  "goal": "프로토타입과 뷰어를 분리하는 「액자(frame) 모드」 — #14(겹침)·#17(폭 시뮬레이터)의 공통 뿌리 해결. 사용자 결정(2026-08-24): 설계 후 바로 진행",
  "issues": [
    {
      "order": 1,
      "issue": [
        17,
        14
      ],
      "type": "feat",
      "release": "v0.15.0",
      "title": "frame 모드 — 앱을 iframe(액자)에 넣고 뷰어(헤더·패널·마커·폭 시뮬레이터)는 밖에",
      "status": "done",
      "plan": {
        "done": true,
        "cause": "overlay 는 앱과 뷰어가 한 창에 살아 (1) 설명 패널이 앱 우측 서랍을 덮고(#14) (2) 폭을 줄여도 미디어쿼리가 안 먹어 시뮬레이터를 둘 수 없다(#17). wrap 은 정적 HTML 을 시트(컨테이너)에 담아 둘 다 해결돼 있다 — 프레임워크 앱에도 같은 구조를 주되, DOM 을 옮길 수 없으니 iframe 으로 담는다",
        "change": "(1) 설정 mode:\"frame\" (명시만, 기본 아님). (2) boot: 자신이 ScreenSpec 이 만든 iframe 안이면(window.frameElement?.dataset.ssFrame, same-origin) 아무 UI 도 만들지 않고 종료 — 재귀·이중 UI 방지. (3) bootWrap 을 frame 옵션으로 재사용: body 자식을 시트로 옮기는 대신 그대로 두고 숨긴 뒤(display:none, ScreenSpec UI 제외) 시트 안에 <iframe data-ss-frame src=현재 URL> 를 100%×100% 로 넣는다. 툴바·모바일/PC 버튼·리사이즈 핸들·설명 패널·목차는 wrap 것을 그대로 쓴다 → 폭 시뮬레이터 자동 획득, 패널 겹침 소멸. (4) createCore 에 ctx.doc(기본 document)·ctx.win(기본 window) 추가: rootEl/targetOf/showRoot/play selector 조회/arrowTo 조회가 ctx.doc 을, 목차 클릭·flow 의 pushState+popstate 가 ctx.win 을 쓴다. 우리 UI(목차·툴팁·토스트)는 계속 바깥 document. (5) overlay 의 detectScreen(라우트 exact→suffix→prefix, 보이는 root 우선) 을 detectScreenIn(win, doc) 공용 함수로 추출해 overlay 와 frame 이 공유. frame 은 iframe load 후 inner window 의 pushState/replaceState 패치 + popstate/hashchange 로 감지, 감지 시 바깥 URL 을 history.replaceState 로 미러링(새로고침해도 같은 화면). (6) frame 좌표: posOf/rectOf = inner rect + iframe 의 시트 내 위치, /scale. inner window scroll(capture)·resize·MutationObserver → 재배치. iframe 이 시트를 꽉 채우므로 시트 스크롤은 0. (7) 누락 경고 watchMissing 의 MutationObserver 대상도 ctx.doc.body. (8) 기존 overlay·wrap 동작 불변 — e2e 기존 71건 그대로 통과가 회귀 기준",
        "accept": [
          "overlay-spa 를 mode:\"frame\" 으로 열면 시트 안에 iframe 1개, 바깥 body 의 앱 DOM 은 숨김, ScreenSpec 툴바·패널·목차 존재, current()==='S-01'",
          "정의서 모드에서 마커가 iframe 안 대상 위에 놓임 (마커 rect 가 대상의 바깥 좌표 ±12px)",
          "툴바 '모바일' → iframe 폭 360, 앱 안에서 미디어쿼리가 실제로 발화(예제에 max-width 규칙 하나 두고 computed 검사)",
          "iframe 안에서 앱 내비 클릭 → current()==='S-09', 바깥 URL 도 /members 로 미러링. 목차에서 S-01 클릭 → iframe 내부 경로가 /home 으로",
          "iframe 안 상세 패널 열기 → current()==='S-03', 닫기 → S-09 (root 감지가 iframe 문서 기준)",
          "▶(play) 버튼이 iframe 안 요소를 클릭한다",
          "설정 없이 iframe 안에 로드된 인스턴스는 UI 를 만들지 않는다(.ss-pill·.ss-toolbar 0개)",
          "기존 e2e 71건 유지 + frame 8건 이상 추가, smoke 통과, lint 통과(mode 값·문서)"
        ],
        "qa": [
          "e2e: overlay 서버 재사용(포트 다르게) + SCREENSPEC 주입에 mode:'frame' → 위 accept 전수",
          "smoke 는 examples 에 frame 예제가 없으므로 e2e 로 대체"
        ],
        "docs": [
          "config.md mode 행에 frame 추가 + 언제 쓰는지(우측 서랍·모바일 폭 확인이 필요한 프레임워크 앱) + 조건(same-origin·주소로 열림)",
          "SKILL §3-B 에 frame 안내 단락(기본은 overlay, 겹침·폭 확인 필요하면 frame). 기기 시뮬레이터 없음 문구를 frame 안내로 교체",
          "README 모드 표에 frame 행",
          "screenspec.js 헤더 주석 모드 설명"
        ]
      },
      "dev": {
        "done": true,
        "model": "opus",
        "summary": "opus 서브에이전트(187k). 이탈 6건 전부 타당(posOf 스케일 공식, 테스트 주입 방식, basePath 경로, 테스트 순서, iframe 재부모 시 src 복원 + 액자 안 하이라이트 CSS 주입, CSS 특이성)"
      },
      "qa": {
        "done": true,
        "evidence": "diff 범위: wrap/overlay 경로 불변(else 블록 그대로), createCore 는 appDoc/appWin 게터. lint 통과. e2e PASS 81 / FAIL 0 (기존 71 유지 + frame 10). smoke 5/5"
      },
      "commit": {
        "done": true,
        "sha": "aa8d7ce"
      }
    },
    {
      "order": 2,
      "issue": [
        17,
        27,
        26
      ],
      "type": "feat",
      "release": "v0.15.0",
      "title": "반응형 문서 모델(baseViewport·viewports·SKILL 원칙) + 현재 미표시 표시 + §7 커버리지 질문",
      "status": "done",
      "plan": {
        "done": true,
        "cause": "#17 코멘트: 쓰는 기준/보는 기준 분리 요청 — 값싼 1·2·3번. #27-1: state 정의가 지금 화면에 없음을 구분 불가. #26: §7 이 안 적은 것을 묻지 않음",
        "change": "RAW.baseViewport 시작 폭, screen.viewports 목차 배지, SKILL §6 원칙 1줄·§7 커버리지 1줄, placeMarkers 에서 행에 ss-now-hidden 토글 + '현재 미표시' 태그",
        "accept": [
          "e2e 3건"
        ],
        "qa": [
          "lint·e2e·smoke"
        ],
        "docs": [
          "config.md 2행",
          "SKILL 2줄"
        ]
      },
      "dev": {
        "done": true,
        "model": "main",
        "summary": "메인 직접 ~40줄"
      },
      "qa": {
        "done": true,
        "evidence": "lint 통과. e2e PASS 84 / FAIL 0. smoke 5/5. baseViewport 검사식 1회 수정(정의서 모드 축소 고려)"
      },
      "commit": {
        "done": true,
        "sha": "(release 커밋에 포함)"
      }
    }
  ],
  "deferred": [
    "panel:\"left\" + 헤더 「패널 ⇄」 버튼 — frame 모드가 안착하면 제거 검토 (사용자: 버튼 불필요)",
    "optional:true 플래그 (#23 후반) — 사용자 결정 대기"
  ],
  "cycle_qa": {
    "done": true,
    "evidence": "lint·e2e 84·smoke 5 전부 통과"
  },
  "release": {
    "done": true,
    "version": "0.15.0",
    "sha": "(아래)"
  }
}

# AGENTS.md

이 저장소(ScreenSpec 라이브러리 자체)를 수정할 때 지켜야 하는 규칙이다.

> **찾는 게 이게 아닐 수 있다.** 사용자의 프로토타입에 ScreenSpec을 *적용*하려는 것이라면
> [SKILL.md](SKILL.md)를 읽어라. 이 문서는 라이브러리 자체를 고칠 때의 규칙이다.

## 구조

| 경로 | 역할 |
|---|---|
| `screenspec.js` | 라이브러리 전부. 단일 파일, 의존성 0, 빌드 없음 |
| `SKILL.md` | AI가 남의 프로토타입에 적용할 때의 작업 지시서 |
| `docs/config.md` | 설정 필드 전체의 단일 출처 |
| `examples/*.html` | 실행 가능한 예제 겸 e2e 대상 |
| `scripts/inline.js` | 라이브러리를 프로토타입 파일 안에 넣어 자체 완결 HTML 생성 |
| `tests/lint.js` | 의존성 없는 정적 검사 |
| `tests/e2e.js` | Playwright 브라우저 회귀 |
| `tests/smoke.js` | 예제 전수 클릭 스모크 (아무거나 눌러도 안 죽는가) |
| `scripts/release.js` | 릴리스 — 태그·푸시·퍼지·실물 확인 (문서가 미출시 태그를 가리키는지 검사) |
| `.github/workflows/release.yml` | 릴리스를 Actions 버튼으로 (같은 검사를 release.js 와 공유) |
| `scripts/backlog-sync.js` | GitHub 이슈 ↔ Notion 보드 싱크 검사 (로컬 전용) |
| `_private/` | **저장소 밖**(gitignore). 사이클 기록·제품 계획 등 내부 문서 — 아래 「저장소에 두지 않는 것」 |

## 판단 기준 — Notion for product managers

이 제품의 슬로건이자 **UI 결정의 기준**이다 (PM 2026-08-30). 프로토타입에 노션을 씌운다.

- 결정이 갈리면 **「노션은 어떻게 하나」를 먼저 본다.** 노션에 있는 개념은 노션의 이름과 손짓을 그대로 쓴다
- **우리가 새 개념을 만들지 않는다.** 기획자가 이미 아는 손짓이면 배울 것이 없다
- 예: 블록 · 슬래시 메뉴 · 거터의 ＋ 와 ⠿ · Enter 는 빈 줄(불릿은 `-` + 스페이스) · 편집 모드 없이 눌러서 고치기
- 우리 것으로 남는 건 **화면과 정의가 붙어 있다는 점** 하나다. 그 밖은 노션을 따른다
- **미니멀이 디폴트다** (PM 2026-08-31). 코드든 기획이든 최소한으로 구현하고, 쓸데없이 늘리지 않는다. 개념·타입·옵션은 **결과가 달라질 때만** 나눈다 — 같은 결과면 하나로 통일한다 (예: anno 8종 → 5종, #63)

## 게이트는 층으로 나눈다 — 전체는 CI 가 돈다 (PM 2026-08-31)

재 봤다: lint 0초 · **e2e 158초** · smoke 22초. e2e 158초 중 **112초가 고정 대기**(waitForTimeout 286회)다.
그리고 **CI 가 이미 push 마다 전체를 3분 31초에 돌고 있다**(그 중 e2e 가 157초). 로컬에서 또 도는 것은 같은 일을 두 번 하는 것이다.

| 언제 | 무엇을 | 시간 |
|---|---|---|
| 고치는 동안 | `node tests/e2e.js --only <섹션>` (목록은 `--list`) | 20초 |
| **커밋 전** | **`node tests/lint.js` + 관련 `--only` 섹션** | 20초 |
| push 후 | **CI 가 lint·e2e 전체·smoke** | 3분 31초 (사람은 안 기다린다) |

- **CI 가 빨간 채로 다음 일을 시작하지 않는다.** 이것이 이 규칙의 유일한 조건이다. push 뒤 `gh run watch --exit-status` 로 본다
- 감수하는 것: 깨진 커밋이 main 에 3분 31초 동안 올라가 있을 수 있다. 혼자 쓰는 라이브러리라 감당한다
- 무거운 검사를 pre-commit 훅에 넣지 않는다 — 넣으면 `--no-verify` 로 우회하게 되고, 그때부터 훅이 없는 것보다 나빠진다
- 자동저장(1.2초)·파일감시(3초)처럼 «제품의 타이머» 를 기다리는 대기는 못 줄인다. 일괄로 깎으면 깨진다(실측)
- e2e 병렬화·브라우저 없는 테스트 분리는 **CI 가 5분을 넘길 때** 손댄다

## 검사를 검사한다 — 초록불을 그냥 믿지 않는다 (PM 2026-08-31)

2026-08-31 실제 사고: #67 의 e2e 4건이 전부 PASS 였는데 목차 클릭 경로는 여전히 깨져 있었다(#74).
시험을 `setScreen()` 으로 재서 «사람이 실제로 누르는 길» 을 한 번도 안 지나갔다. **초록불이 「고쳐졌다」를 뜻하지 않았다.**

- **새 기능·수정에는 돌연변이를 한 줄 같이 등록한다** — `scripts/mutate.js` 의 `MUTS` 에, 그 기능을 죽이는 한 줄 치환으로
- `node scripts/mutate.js` 가 그 결함을 심고 해당 e2e 섹션만 돌린다. **FAIL 이 나야 그 검사가 진짜다**
- **PASS 가 나오면 그 검사는 가짜다.** 무엇을 재는지 다시 쓴다. 넣자마자 가짜 2건을 잡았다(#75·#77 시험)
- 시험이 «멈춰 버리는» 것(기다리던 것이 안 나타나 타임아웃)도 잡은 것이다 — 종료코드로 본다
- 새 e2e 를 쓸 때는 **사람이 실제로 누르는 길**로 쓴다. 내부 함수를 직접 부르면 그 경로만 재고 만다

## 손으로 눌러 보는 QA 는 파일 하나에서 (PM 2026-08-31)

- **`tests/qa/qa.html` 하나**가 QA 하네스다. 시나리오 하나 = 화면 하나 · 목차로 옮겨 다닌다. 탭을 여러 개 열지 않는다
- **QA 절차는 그 화면의 정의서(`defs`)에 적는다** — 오른쪽 패널이 곧 체크리스트다. 제품으로 제품을 검사한다
- 이슈를 고치면 그 화면에 확인 항목을 **더한다**. 새 갈래면 화면을 하나 더한다. 지우지 않는다 — 회귀는 옛 항목에서 나온다
- 뽑기는 `node scripts/qa.js --open`. 산출물 `_qa/qa.html` 은 추적하지 않는다(원본만 추적)
- 자동저장·파일 고르기는 `file://` 로 열어야 보이므로 인라인(자체 완결)으로 뽑는다

## 불변 — 내가 옮긴 것 말고는 안 바뀐다 (R0, PM 2026-08-30)

> 「내가 바꾸는 건 그 하나의 불렛만. 그게 다른 곳을 바꾸면 안 돼. 뎁스나 디펜던시가 바뀌면 안 되는 거야.」

블록을 옮기거나 들이거나 내밀 때, **끌고 간 덩어리 말고는 아무것도 안 바뀌어야 한다** — 남의 깊이도, 남의 소속(부모)도.

**지켜진다 — 구조로.** 정의(`defs`)는 **트리**다: 각 블록이 자기 하위를 `c` 에 직접 들고 있다. 부모가 «담김» 으로 확정되므로 계산하지 않고, 따라서 남의 소속이 바뀔 «길» 자체가 없다.

옮기기는 «담긴 목록에서 빼서 갈 목록에 넣기» 뿐이다. 하위는 그 블록이 들고 있으므로 저절로 따라오고, 남의 목록은 손댈 일이 없다.

`node scripts/qa-drag.js` 가 272자리에서 이것을 잰다 — **지금 0곳**. (평평한 목록 + 깊이 숫자 시절에는 16곳이었다.)

## 저장소에 두지 않는 것 (public repository)

이 저장소는 **지금 나가 있는 판을 설명하는 곳**이다 — 쓰는 법·설정·예제. 그게 전부다.

| 두지 않는다 | 어디에 있나 |
|---|---|
| 제품 로드맵·단계별 상세기획·가격/플랜 | Notion (`_private/product/` 는 로컬 사본) |
| 백로그·우선순위·일정 | Notion 보드 (원본은 GitHub Issues) |
| 사이클 작업 기록·회고·tasks.json | `_private/sprint/` (로컬) |

경쟁사가 읽어서 곤란한 것이 아니라, **아직 없는 기능을 저장소가 약속하는 것처럼 보이는 게 문제**다.
읽는 사람은 지금 받을 수 있는 것만 알면 된다. 앞일은 Notion 에서 관리한다.

`tests/lint.js` 가 이 규칙을 강제한다 — `docs/product/`·`docs/sprint/`·`_private/` 가 git 에 추적되면 FAIL.

## 검증 — 무엇을 어디서 도는가

> 커밋 전에 **전체** 를 도는 규칙은 2026-08-31 에 바뀌었다. 위 「게이트는 층으로 나눈다」 를 먼저 읽어라.

**커밋 전 (로컬 · 20초):**

```bash
node tests/lint.js                 # 의존성 없음, 0초
node tests/e2e.js --only <섹션>     # 건드린 섹션만. 목록은 --list (37개)
node scripts/mutate.js <id>        # 새 기능·수정을 넣었으면 그 돌연변이 하나
```

**push 후 (CI · 3분 31초):** lint + e2e 전체 + smoke + 릴리스 게이트. **빨간 채로 다음 일을 시작하지 않는다.**

**필요할 때만 (기본 꺼짐 — 느리거나, 특정 규칙을 손댔을 때만 의미가 있다):**

```bash
node tests/e2e.js --grid   # 불릿 위계 전수(272자리) — 드래그·Tab·드롭선 규칙을 고쳤을 때
node scripts/qa-drag.js    # 같은 검사 + 사람이 볼 리포트(_qa/drag-report.html) — PM 검수용
```

> ⚠ **`--grid` 는 지금 빨갛다 — 272자리 중 6자리 (#91).** v0.26.0 에서도 같은 6건이라 이번 판이 만든 것이 아니다.
> **네가 깨뜨린 것이 아니니 시간 쓰지 마라.** 손대기 전 상태와 견주려면 그 이슈의 목록을 기준으로 봐라.
> 선택 실행 검사가 «아무도 안 돌리는 동안 조용히 빨개진» 사례다 — 초록불을 본 적 없는 검사는 검사가 아니다.

어느 쪽인지 갈리면 이 기준으로:

| 바꾼 것 | 로컬에서 돌릴 것 (전체는 언제나 CI) |
|---|---|
| 문서·예제·카피 | lint |
| 코드 (기능 추가·수정) | lint + 그 기능의 섹션 `--only` + 새로 등록한 돌연변이 |
| **여러 곳이 쓰는 것** (슬래시 메뉴·행 렌더·저장 경로) | 쓰는 쪽 섹션도 같이 `--only` — 슬래시에 항목을 더했다가 `[edit]`·`[blk]` 를 안 돌려 CI 를 빨갛게 만든 적이 있다 (2026-08-31) |
| CSS·디자인만 | 같다. 「디자인이라 영향 없다」는 틀린 적이 있다: CSS 한 줄 오타가 뒤 규칙 전부를 조용히 죽였다 (v0.24 사고, lint 22 가 그래서 생겼다) |
| 드래그·Tab·드롭선 «위계 규칙» | 위에 더해 `--grid` (또는 qa-drag) |

**섹션만 돌려서는 안 나오는 결함이 있다.** 2026-09-01: 슬래시를 «아무 데서나 열리게» 고쳤더니 `POST /api/items` 처럼 슬래시가 낀 글이 잘렸는데, 그 증상은 **다른 섹션(`[layer]`)의 문자열이 우연히 밟아 준 덕에** 전체 e2e 에서만 드러났다. CI 는 타이밍 때문에 통과했다.
→ **판을 내보내기 전에는 로컬에서도 전체 e2e 를 한 번 돌린다.** 커밋마다가 아니라 «배포 전 한 번» 이다.

**그리고 그 첫 수정이 여전히 경합이라는 것은 돌연변이 판이 잡았다** — 그 돌연변이가 「놓침」으로 나왔다. **타이밍에 기대는 검사는 가짜 초록불을 낸다.** 증상을 누르지 말고 경합 자체를 없애라(그때는 타이머 대신 그 `input` 에서 열었다). 새 기능을 넣었으면 그 하나만이 아니라 **전체 돌연변이도 배포 전에 한 번** 돌린다 — 남의 리팩터링이 내 돌연변이의 «심을 자리» 를 지워 놓기도 한다(같은 날 `brief-slash-off` 가 그랬다).

간헐 도구도 **이 저장소에 둔다** — 규칙(참조 구현)이 곧 라이브러리의 계약이라 코드와 같이 버전이 돌아야 한다. `_private/`(개인 레포)에는 계획·기록만 둔다. 리포트 출력물(`_qa/`)은 gitignore.

## 문서 동기화 (기계가 강제한다)

lint가 아래를 검사하므로, 코드를 바꾸면 문서도 같이 고쳐야 통과한다.

- 설정 필드를 추가·삭제하면 `docs/config.md`에 반영 (필드 커버리지 검사)
- `ANNO` 레지스트리를 바꾸면 `README.md`·`docs/config.md` 표에 반영
- README의 빠른 시작 예제는 e2e가 실제로 실행한다. API를 바꾸면 예제도 고쳐야 한다
- 폐기한 설계 용어를 문서에 남기지 않는다 (드리프트 검사)
- 버전은 헤더 주석·워터마크 배지·문서 CDN 태그가 한 계열이어야 한다

## 백로그 싱크 (GitHub Issues ↔ Notion 보드)

GitHub 이슈가 원본이고, Notion 보드는 우선순위 판단용이다. 둘이 어긋나면 판단이 틀어지므로 검사로 막는다.

```bash
node scripts/backlog-sync.js           # 드리프트 보고 (기본 dry-run). 어긋나면 exit 1
node scripts/backlog-sync.js --apply   # 노션 쪽을 맞추고 실행 후 자동 재검증
```

잡는 것: 열린 이슈에 카드 없음 · 카드가 있어야 할 칸에 없음 · 죽은 링크.
`NOTION_API_KEY`는 `.env.local`에서 읽는다 (커밋 금지). CI에는 넣지 않는다 — 토큰이 필요한 로컬 검사다.

## 백로그 보드 (Notion) — 컨베이어

**백로그 → 검수 → 완료**, 그리고 「안 한다」로 뺀 것만 **보류**.

| 칸 | 뜻 | 무엇으로 옮겨지나 |
|---|---|---|
| 백로그 | 이슈만 있다 | 사람이 **GitHub 이슈를 연다** — `backlog-sync` 가 카드를 만든다 |
| 검수 | **고쳤고 커밋이 main 에 있다. 이슈는 아직 열려 있다** | 커밋 제목의 `(#N)` 을 `backlog-sync` 가 읽는다 |
| **완료** | **배포됐다 (= 이슈가 닫혔다)** | `release.js --bump --apply` 가 CHANGELOG 최상단의 `#N` 을 닫고 카드를 옮긴다 |
| 보류 | 「안 한다」 | 사람이 노션에서 직접. 기계는 안 건드린다 |

> **커밋 메시지에 `fix #N` 을 쓰지 않는다 — `(#N)` 이다.** `fix` 는 push 순간 이슈를 닫아 버려서
> PM 이 만져 볼 자리가 사라진다. **이슈를 닫는 곳은 배포 한 곳뿐이다** (2026-09-01 PM 결정, #88).
> 곁들여, `fix #N` 자동 종료는 토큰 권한이 모자라면 **조용히 실패**한다 — 기대면 안 되는 장치다 (2026-08-31 에 실제로 4건이 그렇게 열려 있었다).

- **큰 일은 마일스톤으로 묶는다**: GitHub 마일스톤 = 보드 카드 1장(시나리오), 그 안의 이슈 = 카드 속 체크 1줄. 보드에는 시나리오만 보이고 눌러야 세부가 나온다 — 세부까지 카드로 만들면 보드가 죽는다. 체크·진행(n/m)은 `backlog-sync` 가 이슈 상태로 맞춘다
- **검수 칸이 있는 이유**: 만든 사람이 「됐다」고 선언하는 것은 검증이 아니다. PM 이 직접 만져 보고 피드백을 줄 자리를 남긴다 (2026-08-29 PM 결정). **「배포해 달라」는 말이 곧 인수 신호**다 — 그때까지 이슈는 열려 있고, 피드백은 그 이슈에 그대로 쌓인다
- **만든 것은 PM 이 볼 수 있는 형태로 건넨다** — `node scripts/inline.js <예제> -o <경로>` 로 자체 완결 HTML 을 뽑아 전달하고, 무엇을 봐 달라는지 한 줄로 적는다. 검수 칸에 카드만 올려 두고 끝내지 않는다
- 진행 중 칸을 두지 않는다 — 1인 작업에서 「지금 하는 것」은 칸이 아니라 지금 열어 둔 창이다
- 완료 카드는 지우지 않고 쌓는다 (GitHub Release 가 원본, 보드는 흐름을 보여 준다)

## 릴리스

1. CI 초록 확인 (push 마다 전체가 돈다) + **로컬에서 전체 e2e 와 전체 돌연변이를 한 번씩** — 위 「검증」의 2026-09-01 사고 참조
2. `CHANGELOG.md`에 항목 추가 (최상단 `## vX.Y.Z`) — **사람이 쓰는 유일한 부분**이다. 판 번호도 여기서 정해진다
3. `node scripts/release.js --bump --apply` 한 줄. 버전 일곱 곳 → 커밋 → 태그 → 푸시 → 퍼지 → GitHub Release → 실물 확인까지 간다

> 일곱 곳: `screenspec.js` 헤더 1 · 워터마크 배지 2 · `console.info` 2 · `README.md` CDN 태그 1 · `SKILL.md` CDN 태그 1.
> `--bump` 는 그 일곱 곳을 «CHANGELOG 최상단» 에 맞추는 일만 한다 — 다음 판이 무엇인지는 사람이 이미 적었다.
> 개수가 예상과 다르면 아무것도 안 고치고 멈춘다. 버전과 태그가 **같은 판에** 나가므로 예전의 «빨간 창» 도 사라진다.
> `--apply` 없이 `--bump` 만 주면 무엇을 고칠지 보여 주기만 한다. CHANGELOG 한 파일만 미커밋인 것은 봐 준다(그 변경은 release 커밋에 담긴다).

버전이 이미 올라가 있으면 `--bump` 없이 아래 둘 중 하나로도 된다.

**A. Actions 버튼** (터미널 없이 · 태그 푸시 권한이 없는 환경에서도)
Actions 탭 → **release** → *Run workflow* → 버전(`vX.Y.Z`) 입력.
버전 정합 → lint·e2e·smoke → 태그+Release 발행 → 퍼지 → 실물 검증까지 한 번에 돈다.

**B. 로컬**

```bash
node scripts/release.js           # 검사만 — 나갈 준비가 됐는지
node scripts/release.js --apply   # 태그 → 푸시 → jsDelivr 퍼지 → 실물 확인
```

5. B 로 냈다면 `gh release create vX.Y.Z --verify-tag --notes-file <(node scripts/release.js --notes)` 로 Release 를 발행한다. A 는 이 단계까지 자동이다

> **버전만 올려 push 하면 main 이 잠깐 빨개진다** — 문서가 아직 없는 태그를 가리키므로 릴리스 게이트가 걸린다.
> 태그를 만들면 풀린다(실패한 판은 `gh run rerun` 으로 다시 돌린다). #86 으로 버전 커밋과 태그가 같이 나가면 이 창이 사라진다.

> **`@0` 은 파일이 먼저, 메타 API 가 나중이다.** 퍼지 직후 실물 파일은 새 판을 서빙하는데
> `data.jsdelivr.com/…/resolved` 는 며칠 옛 번호를 답한다. 확인은 «실물 파일 헤더» 로 한다 (2026-08-31).

> **CI 가 상시 감시한다.** main 에 push 될 때마다 `node scripts/release.js --tag-gate` 가 돌아
> 「문서가 가리키는 버전의 태그가 원격에 실재하는가」를 본다. 릴리스를 안 하면 main 이 빨강으로 남는다 —
> 버전만 올려 두고 잊는 실패는 «릴리스를 낼 때만» 도는 검사로는 잡히지 않기 때문이다. (2026-08-28, 3회 재발)

> **버전 문자열만 올리고 태그를 만들지 않으면 안 된다.** 그 순간 README·SKILL 이 존재하지 않는 CDN 주소(`@vX.Y.Z` → 404)를 가리키고,
> 그 주소를 심은 프로토타입은 조용히 아무것도 안 뜬다. lint 는 이걸 못 잡는다 — 헤더·배지·CHANGELOG·문서 태그가 *서로 같은지*만 보지
> 그 태그가 *실재하는지*는 보지 않기 때문이다(의존성 0 정적 검사). `scripts/release.js` 가 그 구멍을 막는다. (2026-08-25)


## 코드 규칙

- 의존성·빌드 단계를 추가하지 않는다. 단일 파일 유지가 이 프로젝트의 제약이다
- CSS는 `:where()`로 리셋해 호스트 페이지 스타일과 싸우지 않는다 (특정도 0)
- 액센트 계열 색을 하드코딩하지 않는다. `--ss-accent` 토큰과 `color-mix`만 사용 (lint가 검사)
- overlay 모드 UI는 브라우저 최대 z 대역에 둔다. 앱의 z-index와 경쟁하지 않는다
- 사용자 문자열은 `esc()`로 이스케이프한다

## 이슈

버그·요청은 https://github.com/charmisuk/screenspec/issues 로. 제목은 `[버그]` 또는 `[요청]` + 한 줄 요약.

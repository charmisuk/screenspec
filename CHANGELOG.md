# Changelog

## v0.25.0 (2026-08-30) — Notion for product managers

**슬로건을 판단 기준으로 삼았다.** PM: 「제품의 마케팅 슬로건을 'notion for product managers' 로 잡고 싶어. 프로토타입에 노션을 씌우는 거야.」 앞으로 UI 가 갈릴 때 「노션은 어떻게 하나」를 먼저 본다.

### 전부 블록이다 (#55·#56·#57·#58)

기능 설명 밑이 통째로 하나의 에디터고, 그 안의 모든 것이 블록이다. 번호도 블록(콜아웃)이고 그 안에 불릿 블록이 들어간다.

- **정의를 «평평한 블록 목록» 으로.** 옛 문서의 `subs` 중첩과 `why` 속성은 열 때 **들여쓰기를 가진 형제 블록**으로 펴진다. 표현이 하나뿐이라 편집·드래그·저장이 전부 같은 규칙으로 돈다
- **Enter 는 그냥 빈 줄.** 글머리표가 자동으로 안 생긴다. `-` + 스페이스나 `＋` · `/` 로 불릿이 된다 (노션과 같다)
- **「이유:」 라벨 제거.** 화살표(`↳`)만 남고, 화살표도 다른 블록처럼 옮기고 지운다. `>` 로도 만든다
- **편집 모드 폐지.** 켜고 끄는 상태가 없다. 글자를 누르면 그 자리에서 고쳐진다. `readonly: true` 만 잠근다
- 저장·설정 복사는 위 툴바로 옮겼다. 패널 머리는 «쓰는 자리» 라 서식이 온다

### 규격을 노션에서 실측해 조였다 (PM 2026-08-29)

노션 페이지의 실제 DOM 에서 수치를 재고, 좁은 패널에 맞게 4분의 3으로 줄였다.

| | 노션 | ScreenSpec |
|---|---|---|
| 글머리 칸 | 24px | 16px |
| 들여쓰기 한 단 | 24px | 16px |
| 블록 위·아래 | 8px (사이 16px) | 3px (사이 6px) |
| 줄 높이 | 24px | 20px |

**글머리 칸 = 들여쓰기 한 단**으로 못박았다. 안쪽 블록의 글머리가 바깥 블록의 글자 자리에 정확히 선다(노션은 여기가 6px 어긋나 있다). 숫자는 CSS 변수 다섯 개에 모아 두어 한 곳만 바꾸면 전체가 따라온다.

### 드래그에 위계 규칙을 넣었다 (#61)

PM: 「같은 위계의 불릿과 그 밑에 들어가는 서브 불릿이 있는데 그거에 대한 명확한 규칙이 없는 것 같아.」

- **부모를 끌면 딸린 하위가 통째로 따라온다** — 하나만 떨어져 나가지 않는다
- **좌우로 밀면 들어갈 깊이가 바뀐다.** 드롭선의 왼쪽 끝이 그 깊이다
- **바로 앞 블록보다 한 단까지만.** 허공에 뜬 하위를 만들 수 있는 자리는 애초에 주지 않는다
- **번호와 번호 사이에는 드롭선을 그리지 않는다** — 되지도 않는 자리를 보여 주면 안 된다
- 드롭선은 «실제로 들어갈 자리» 에 그린다. 부모 위에 놓으면 그 하위 뒤로 가므로, 커서 바로 밑에 그리면 눈과 결과가 어긋난다

### 밖에서 바뀐 파일을 알아챈다 (#63, PM 2026-08-30)

PM: 「에이전트가 수정하면 반영이 안 돼?」 파일은 바뀌는데 브라우저는 모른다. 모르는 채로 자동저장이 돌면 **그 변경을 덮어쓴다.**

- 자동저장을 켜면 그 파일의 손잡이를 쥐고 있으므로 **언제 바뀌었는지** 물어볼 수 있다
- **탭이 보일 때만 3초마다**, 탭이 가려지면 멈추고, **돌아오는 순간 즉시** 한 번 본다. 사람은 그동안 에이전트 쪽을 보고 있으므로 «돌아오는 순간»이 가장 중요한 확인 시점이다. 비용은 파일 정보 한 번 읽기다 (내용은 안 읽는다)
- 감지하면 **자동저장을 멈추고** 패널 안쪽에 띠를 띄운다: 「프로토타입 파일이 밖에서 바뀌었습니다 · 새로고침 / 내 것 유지」
- 프로토타입 위에 뜨는 팝업이 아니다 — 「프로토타입의 동작을 방해하지 않는다」가 이 제품의 전제다

**저장 단추도 상태를 갖는다** (PM 제안): 자동저장이 켜져 있고 저장까지 끝났으면 **꺼진다**. 누를 일이 없는 단추를 누를 수 있는 것처럼 두지 않는다.

### 찍은 번호가 저장하면 죽던 버그 (2026-08-30)

**FTUE 의 약속이 새로고침 한 번에 무너지고 있었다.** 「화면에서 번호 찍기」는 화면의 요소에 `data-spec` 이름표를 붙이는데, 저장은 설정 블록만 갈아끼우므로 **그 이름표가 파일에 남지 않는다.** 저장하고 다시 열면 마커가 통째로 사라지고 「현재 미표시」가 됐다.

- 찍을 때 **그 요소를 되찾을 선택자(`sel`)를 설정에 같이 적는다.** id 가 있으면 `[id="buy"]`, 없으면 뿌리에서부터의 짧은 경로
- 열 때 이름표가 없으면 선택자로 찾아 **이름표를 저절로 되붙인다**
- 덤으로 **AI 가 프로토타입을 고치다 `data-spec` 을 지워도 같은 길로 저절로 돌아온다** — 「지우지 마라」는 규칙의 안전망이 생겼다

**같이 잡힌 것: 라이브러리 안에서 `CSS.escape` 가 죽어 있었다.** 이 파일에는 스타일시트를 담은 `const CSS` 가 있어서 브라우저의 전역 `CSS` 를 가린다. `CSS.escape(...)` 는 예외를 던지고 `try` 안에서 조용히 삼켜지고 있었다. id 선택자 대신 `[id="..."]` 를 쓴다.

### README 를 처음 오는 사람 기준으로 다시 썼다 · 「설정 복사」 → 「설명 복사」

**README 가 코드 덩어리로 시작하고 있었다.** 정작 이 도구의 시작점(화면에서 번호 찍기)은 한참 아래에 한 줄로 있었다.

- 맨 앞에 **「30초 만에 감 잡기」** 5줄 — 붙이고 · 켜고 · 찍고 · 쓰고 · 저장
- **「이걸 왜 쓰나」** 를 문제부터: 산출물이 둘이면 반드시 어긋난다
- **「이 도구가 지키는 세 가지」** 신설 — 동작 방해 안 함 · 내가 옮긴 것만 바뀜(R0) · 문서와 실물이 같은 파일
- **AI 와의 핑퐁**을 그림으로: 쓴다 → 저장 → AI 가 고침 → 띠 → 새로고침
- 예시를 트리(`c`)로 갱신. 자동저장·파일 감지·「설명 복사」를 「쓰는 법」 한 표에 모음
- 「자주 막히는 곳」에 새 증상 추가 (찍은 번호가 안 남는다 · 자동저장이 안 눌린다)

**「설정 복사」를 「설명 복사」로.** 복사되는 것은 «설정» 이 아니라 «지금까지 쓴 기능 설명» 이다. 자동저장이 안 되는 브라우저를 위한 우회로라는 것도 안내에 적었다.

### 정의를 «트리» 로 옮겼다 — R0 을 구조로 지킨다

PM: 「내가 바꾸는 건 그 하나의 불렛만. 그게 다른 곳을 바꾸면 안 돼.」

**코드로는 못 지키는 약속이었다.** 정의가 «평평한 목록 + 절대 깊이 숫자» 라 부모가 저장되지 않고 «앞쪽에서 한 단 얕은 첫 줄» 로 계산됐다. 앞에 뭐가 끼면 뒤 줄의 부모가 조용히 바뀐다 — 272자리 중 16자리.

이제 **각 블록이 자기 하위를 직접 들고 있다.**

```
전 →  { t:"A" }, { t:"B", indent:1 }, { t:"C", indent:2 }
후 →  { t:"A", c:[ { t:"B", c:[ { t:"C" } ] } ] }
```

부모가 «담김» 으로 확정되므로 계산하지 않는다. 그래서 **남의 소속이 바뀔 길 자체가 없다.**

- **옮기기는 «빼서 넣기» 뿐이다.** 하위는 그 블록이 들고 있으므로 저절로 따라오고(R7), 남의 목록은 손댈 일이 없다(R0)
- **없어진 것**: 깊이 숫자(`indent`) · 깊이 자르기 · 「내 하위 어디까지?」 계산 · 「앞 줄보다 한 단까지」 방어 규칙의 절반
- **저장 파일이 작아졌다** — 줄마다 붙던 `indent:1` 이 사라져 중첩 괄호보다 절약이 크다
- **옛 문서 3종(깊이 숫자·`subs`·`why`)** 은 부팅 때 담김 관계로 세워 읽는다. 그대로 열린다

**검증을 «결과 예측» 에서 «불변 조건» 으로 바꿨다.** 트리에서는 구현을 베껴 결과를 예측하는 것이 검증이 아니다 (같은 코드를 두 번 쓰는 셈). 대신 무슨 일이 있어도 지켜져야 하는 것을 잰다:

| 불변 | 뜻 |
|---|---|
| R0 남이 안 바뀜 | 내가 옮긴 것 말고는 깊이도 소속도 그대로 |
| 개수 보존 | 옮기다 줄이 사라지거나 늘면 안 된다 |
| 하위 동행 | 딸린 하위는 통째로 따라온다 |
| 깊이 상한 | 2단을 넘는 줄이 생기면 안 된다 |
| 표시 = 변화 | 선이 뜨면 반드시 바뀌고, 안 뜨면 반드시 그대로 |

**272자리 전부 통과 · R0 위반 0곳** (평평 시절 16곳).

옮기는 중에 잡힌 것: 하위를 든 채로 깊이 들어가면 그 하위가 3단이 되던 것 · 걸러 그린 화면(개발만)에서 편집이 «걸러진 순번» 으로 써서 남의 줄을 덮던 것 — 둘 다 전수 검증이 잡았다.

### R0 «내가 옮긴 것 말고는 안 바뀐다» 를 철학에 못박고, 빚을 세기 시작했다

PM: 「우리 룰은 어떤 행동을 하더라도 기존 구조를 안 건드는 거야. 뎁스나 디펜던시가 바뀌면 안 돼. 이건 철학에 들어가 있는 거지?」

**들어가 있지 않았다.** 규칙 R1~R7 은 «어디에 · 몇 단으로 놓이는가» 만 정했지, «남은 그대로여야 한다» 는 없었다. 이제 **R0** 로 맨 위에 올렸다.

그리고 **지킬 수 없다는 것도 같이 적었다.** 정의가 «평평한 목록 + 절대 깊이 숫자» 라 부모가 저장되지 않고 계산된다 — 앞에 뭐가 끼면 뒤의 부모가 조용히 바뀐다. 코드로 못 막는다.

`qa-drag.js` 가 이제 그 빚을 센다: **272건 중 16건**. 예:

| 목록 | 끈 것 | 결과 | 남이 바뀜 |
|---|---|---|---|
| `A, B(1), C, D(1)` | B 를 0단으로 | `A, C, B, D(1)` | **D 의 부모 C → B** |
| `A, B, P(1), Q(1)` | A 를 B 뒤로 | `B, A, P(1), Q(1)` | **P·Q 의 부모 B → A** |

세는 규칙도 정확히 했다: **끌고 간 덩어리(자기 + 딸린 하위)는 «내가 바꾼 것»** 이라 위반이 아니다 (R7). 이 구분을 안 하면 38건으로 부풀어 보인다.

**16 → 0 이 트리 전환의 합격 기준이다.**

### 소속 박스를 «반듯한 사각형 하나» 로 (PM QA 2026-08-30)

PM: 「노션은 그 불렛을 포함해서 그 밑에 불렛을 다 하이라이트 쳐주는데 우리는 좀 다른 것 같아. 철학 문제인지 코드 문제인지 봐줘.」

**철학과 규칙은 맞았고, 그리는 방법이 틀렸다.** 계산으로 확인하니 감싸는 범위(부모 + 그 하위 전체)는 노션과 정확히 같았다. 문제는 **블록마다 배경을 칠하고 있었다**는 것 — 블록에 들여쓰기 여백이 있어서 왼쪽 끝이 계단처럼 어긋났다 (실측 789 → 805 → 821). 노션은 하나의 반듯한 사각형이다.

**선과 같은 길로 바꿨다** — 떠 있는 판 하나로 덮는다. 덕분에:

- 계단이 사라지고 노션과 같은 모양이 됐다
- CSS 클래스 3개(`ss-drop-in-a/-z` 등)와 클래스를 붙였다 떼는 로직이 없어졌다
- 그리는 길이 «판 하나 띄우기» 하나로 합쳐졌다 (선·박스 공용)

박스는 그 번호 «안» 에 둔다 — 목록에 두면 번호 카드의 배경에 가려진다. 글 뒤·카드 배경 앞이 제자리다.

박스에도 «누구를 감쌌는가» 를 값으로 적어 둔다. 검사가 DOM 구조를 되짚지 않으므로 표시 방법이 또 바뀌어도 안 깨진다.

### 🚫 가 깜빡이던 진짜 원인 — dragenter (PM QA 2026-08-30)

PM: 「블럭과 블럭 사이를 지나는데 가끔씩 없음이 뜨거든? 떴다가 바로 없어져. 애초에 안 떠야 맞지 않아? 뭔가 예외처리로 넘어가는 느낌인데.」

맞았다. 예외처리였다. **`dragover` 만 막고 `dragenter` 를 안 막고 있었다.**

HTML5 끌어놓기는 «이 요소가 받을 수 있는가» 를 **`dragenter` 에서** 정한다. 안 막으면 새 요소로 들어가는 **그 한 프레임** 동안 🚫 가 떴다가, 이어지는 `dragover` 가 막으면서 사라진다. 블록과 블록 사이를 지날 때마다 요소가 바뀌므로 그때마다 깜빡였다.

실측: 고치기 전 **`dragenter` 31프레임이 전부 안 막히고 있었다** (`dragover` 는 20프레임 전부 막힘). 고친 뒤 66프레임 중 🚫 조건 **0건**.

**판정을 이벤트에서 좌표로 바꿨다.** 목록을 벗어났는지를 `dragleave` 로 보고 있었는데, 블록 사이를 지날 때도 «떠났다» 로 잡히는 경우가 있어(relatedTarget 이 비어 온다) 그림이 지워졌다 다시 그려졌다. 좌표는 그런 사정이 없다 — 안이면 안이고 밖이면 밖이다. `dragleave` 는 아예 없앴고, 한 프레임을 정하는 곳도 한 군데로 합쳤다.

**시험 하네스도 같이 고쳤다.** 잡는 자리를 500px 로 박아 뒀는데 그건 패널 «밖» 이다 — 사람은 손잡이를 패널 안에서 잡는다. 좌표 판정을 넣자 이 가정이 드러났다. 이제 실제 손잡이 자리에서 시작한다.

### 드래그를 «계산 · 그리기 · 실행» 으로 다시 세웠다 (PM 2026-08-30)

PM: 「근본적인 원인을 픽스하고 뼈대를 잘 세우는 형식으로. 그냥 코드 땜빵하는 식으로 말고. 우리 철학 세우고 그 철학대로 가기로 했잖아.」

맞는 지적이었다. 드래그 처리가 리스너 6곳에 흩어져 있어서, 문제가 나올 때마다 «그 리스너» 만 고치고 있었다. 그래서 한 곳을 고치면 다른 곳이 어긋났다 (🚫 를 목록 안에서만 없앤 것, 표시가 레이아웃을 밀던 것 모두 같은 뿌리).

**철학을 코드 앞에 적고, 구조를 그 철학대로 세웠다.**

1. 표시는 **자리를 차지하지 않는다** — 무엇을 그려도 글은 한 픽셀도 안 움직인다
2. 표시는 **«놓으면 무엇이 되는가» 만** 말한다 — 박스=소속 · 선=자리 · 아무것도 없음=안 바뀜
3. 브라우저의 **«놓을 수 없음(🚫)» 은 끄는 내내 한 번도 안 나온다**
4. **커서는 자유롭다** — 가장 가까운 자리로 붙는다

한 프레임에 딱 세 걸음이다. 각자 하는 일이 하나뿐이라 «어디를 고쳐야 하는지» 가 자명하다.

| 걸음 | 하는 일 | 안 하는 일 |
|---|---|---|
| `dragPlan(e)` | 순수 계산 — 계획 또는 null | DOM 을 바꾸지 않는다 |
| `dragPaint(p)` | 그리기 | 모델을 안 건드린다 |
| `dragApply(p)` | 모델 변경 | 화면은 render 가 맡는다 |

위계 규칙 7줄(R1~R7)도 이 블록 머리말에 «맞다의 정의» 로 못박았다 — `scripts/qa-drag.js` 가 같은 규칙으로 272가지를 전수 검증한다.

**QA 서버가 캐시를 막지 않고 있었다.** 브라우저가 옛 라이브러리를 쥔 채로 있어서, 고친 것을 «안 고쳐졌다» 로 보게 만들었다. `no-store` 와 빌드 지문(`X-Build`) 을 붙였다.

### 표시가 떨던 버그 — 되먹임 진동 (PM QA 2026-08-30)

PM: 「드래그할 때 막 마이크로 초 단위로 블럭 표시가 떴다가 없어져.」

**표시가 레이아웃을 건드리고 있었다.** 드롭선을 목록의 «흐름 안» 에 끼워 넣었더니 넣는 순간 아래 블록이 2px 밀리고 → 커서 밑 요소가 바뀌고 → 가장 가까운 경계가 달라져 선이 다른 자리로 다시 그려지고 → 또 밀리고… 마우스를 움직이는 내내 이 되먹임이 돌았다. 박스의 여백 조정도 같은 일을 했다.

- **선은 목록 «위에 떠 있는» 채로 그린다** (absolute). 무엇을 그려도 글은 한 픽셀도 안 움직인다
- **박스는 배경만 칠한다** — 여백을 건드리지 않는다
- 선에 «몇 단인지» 를 값으로 적어 둔다 — 검사가 픽셀로 되짚지 않게 (표시 방식이 바뀌어도 안 깨진다)

같은 지점에 10번 신호를 보내도 늘 같은 자리를 가리키는지, 표시가 뜬 동안 글이 한 픽셀이라도 움직이는지를 검사가 본다.

### 드롭 표시를 다시 세웠다 — 박스와 선 (PM 영상 분석 2026-08-30)

PM 이 노션과 우리 것을 나란히 녹화해 주었다. 1초 단위로 보니 노션은 **선만 그리는 게 아니라 박스도 같이 그린다.**

- **박스 = 소속.** 어느 덩어리 «안» 으로 들어가는지를 **부모 + 그 하위 전체**를 감싸 보여준다 (전에는 부모 한 줄만 밝혔다)
- **선 = 자리.** 그 안에서 정확히 어디인지
- **박스가 없으면 최상위** — 어느 덩어리에도 안 속한다는 뜻

노션은 여기에 연한 선과 진한 선을 겹쳐 그리는데, PM 지적대로 그건 지저분하다. **우리는 선 하나만** 쓴다 — 박스가 소속을 말하므로 선이 둘일 이유가 없다.

**「특정한 위치 아니면 안 뜬다」도 고쳤다** (PM QA). 두 가지가 겹쳐 있었다.

- 블록 «위» 에 정확히 올려야만 자리가 잡혔다 → 이제 **커서에서 가장 가까운 경계**로 붙는다. 먹통 구간이 없다
- 무효한 자리에서 브라우저의 🚫 커서가 떴다 → 끄는 동안에는 **문서 어디서든** 이벤트를 받는다. 무효는 «선을 안 그리는 것» 으로 충분하고, 🚫 는 «고장» 처럼 읽힌다. 처음엔 패널 목록 «안» 에서만 받게 고쳤는데, 목록 밖(프로토타입·패널 머리·툴바) 위에서는 여전히 떴다 (PM 2차 QA) — 노션은 끄는 내내 그 표시가 없다

**규칙도 하나 줄었다.** 「부모의 아래쪽 절반이면 그 하위까지 건너뛴다」를 없애고 **«눈에 보이는 줄과 줄 사이» 하나**로 통일했다. 가장 가까운 경계로 붙게 된 이상 그 규칙은 오히려 눈과 결과를 어긋나게 했다.

### 위계 전수 검증 — 뿌리 하나를 찾았다 (PM 2026-08-30)

PM: 「노션 로직을 먼저 상세히 정리하고, 그대로 재현되는지 하나하나 체크하는 게 맞지 않나.」 맞는 지적이었다. 규칙을 **참조 구현**으로 적어 두고 자리 × 위아래 × 깊이를 **204가지 전수**로 돌려 실제와 대조하니 **17건이 어긋났고, 원인은 하나**였다.

**끌고 있는 덩어리를 「앞 블록」으로 세고 있었다.** 깊이 상한과 부모를 정할 때 자기 자신을 앞 블록으로 보면:

- **자기가 자기 하위가 된다** — `0A,1B` 에서 B 를 끌면 B 를 부모로 삼아 2단이 됐다
- 그래서 **위·아래 양쪽에 선이 다 떴다** (PM 관찰)
- 부모를 끌면 **자기 자식이 자기 부모**가 되어 깊이가 어긋난 채 굳고, 그 뒤로 하위가 안 따라오는 것처럼 보였다 (PM: 「다시 옮기면 그게 안 가는데」)

끌고 있는 덩어리를 **「이미 빠진 셈」**으로 보게 고치니 204건이 전부 맞았다. 이 전수 검증은 e2e 에 남아 있다 — 규칙이 흔들리면 200건 중 하나만 어긋나도 걸린다.

### 드롭선을 노션 규칙으로 (#62, PM 2026-08-30)

PM 이 노션에서 불릿을 끌어 옮기는 화면을 녹화해 주었다. 0.25초 단위로 보니 규칙이 분명했다: **자기 자리 근처에서는 선이 아예 안 뜬다.** 커서가 같은 세로 위치에 있어도 가로로 밀어 깊이가 바뀌는 순간에만 나타난다.

PM: 「지금은 그냥 모든 곳에서 다 뜨는 것 같고 그래서 버그가 좀 발생하는 것으로 보여.」

- **놓아도 바뀌는 게 없으면 안 그린다.** 자리도 깊이도 그대로면 표시하지 않는다
- **갈 수 없는 자리(자기 하위 안)도 안 그린다.** 전에는 선을 그려 놓고 놓으면 조용히 무시했다 — **선이 떴는데 아무 일도 안 일어나는 것**이 「버그 같다」의 정체였다
- **하위로 들어가면 부모가 될 블록을 통째로 밝힌다.** 형제로 붙는 것과 눈으로 갈린다 (노션과 같은 방식, 색은 우리 액센트)
- 바뀌는 게 없는 자리로 커서가 들어오면 **앞서 그린 선도 지운다** — 안 지우면 옛 선이 남아 「여기 놓으면 된다」로 읽힌다

이제 **선이 뜨면 반드시 무언가 바뀐다.**

### PM QA 에서 나온 것 (2026-08-30)

**Tab 이 잡아 끌 때와 다른 규칙으로 돌고 있었다.** PM: 「같은 위계 불릿과 그 밑에 들어가는 불릿이 될 때도 있고 안 될 때도 있다.」 원인은 Tab 이 그 줄의 숫자만 1 올리고 있었던 것이다. 네 가지가 깨져 있었다.

- 맨 앞 줄에서 Tab → **부모 없는 하위**가 생겼다
- Tab 을 두 번 → 0단 밑에 **2단**이 생겼다 (한 단 건너뜀)
- 하위 달린 줄을 들이면 **자식이 제자리에 남아** 부모와 같은 단이 됐다
- Shift+Tab 으로 나오면 **손자가 두 단 떨어진 고아**가 됐다

이제 규칙은 하나다 — **바로 앞 블록보다 한 단까지 · 딸린 하위는 통째로 따라온다.** Tab 도 드래그도 같은 규칙을 쓰고, e2e 가 둘을 같이 지킨다.

**글머리가 글자보다 살짝 위에 떠 있었다.** PM 지적. 줄 상자의 한가운데에 글머리를 두었는데, 한글은 밑선 위로 넓게 앉아 **잉크의 중심이 줄 상자 중심보다 아래**다. 실측(12.5px/20px) 0.88px. 글머리를 그만큼 내렸고, `↳` 는 글리프 자체가 1px 더 높이 앉아 그만큼 더 내렸다. 눈대중이 아니라 캔버스로 잉크 상자를 재서 정했다.

### 에이전트 인계 (PM 지적 2026-08-30)

「깃 풀 → 에이전트와 대화 → 제품 켜기」 흐름을 점검하니 **인계에 구멍**이 있었다.

- **SKILL.md 가 없는 UI 를 설명**하고 있었다 — 「편집」 버튼(폐지)·`why` 속성·`subs` 중첩
- **`data-spec` 을 지우지 말라는 규칙이 없었다.** 에이전트가 프로토타입을 다시 고치면서 표식을 지우면 기획자가 써 둔 설명이 「현재 미표시」가 된다. 재매칭 화면보다 이 규칙이 먼저다
- **인계 보고에 「이제 할 수 있는 것」이 없었다.** 사용자가 초안을 받고도 «누르면 고쳐진다 · 번호를 직접 찍는다 · 자동저장을 켠다» 를 모른 채 끝났다. 네 줄짜리 안내를 필수로 박았다
- `llms.txt` 도 `parts`·`why`·`1a·1b` 를 설명하고 있었다. **폐기 용어 검사가 llms.txt·AGENTS.md·docs/config.md 를 안 보고 있던 구멍**을 막았다 (「없앴다」고 적은 줄은 통과시킨다)

### 처음 오는 사람 (FTUE)

- **빈 화면이 「코드를 이렇게 고치세요」 대신 「화면에서 번호 찍기」 단추를 준다.** 기획자가 코드를 안 열고도 첫 번호를 붙일 수 있어야 이 도구가 시작된다
- 번호를 붙이면 곧바로 이름 칸에 커서가 간다. 이름은 **비어 있고 자리안내만** 뜬다 — 「새 영역」 이 진짜 글자로 박혀 있어 타이핑이 그 뒤에 붙던 것을 고쳤다
- 이름에서 Enter 를 치면 이미 있는 빈 줄로 간다 (빈 줄을 둘 만들지 않는다)
- **쓴 글이 사라지던 버그 (PM 발견).** 「바깥을 누르면 반영」 판정이 패널 목록 «안» 에서만 돌아서, 패널 머리·툴바·프로토타입을 누르면 방금 쓴 글이 저장되지 않은 채 다음 렌더에 사라졌다. 이제 어디를 눌러도 반영된다
- **FTUE 다섯 걸음을 e2e 가 실제로 걷는다** — 빈 화면 → 번호 찍기 → 조준 → 번호 붙이기 → 이름·설명 쓰기까지

### 로컬 자동저장 (PM 2026-08-29)

PM: 「번호 넣고 입력하면 구글 시트 자동저장되듯이 로컬에 계속 저장되면서 가면 그게 프로토타입 파일에 반영되고 그걸 또 클로드가 픽스하고 이렇게 서로 핑퐁이 되지 않을까.」

- **파일을 한 번 고르면 그 뒤로는 알아서.** 손이 멈추면(약 1.2초) 프로토타입 HTML 의 설정 블록에 바로 쓴다
- **오른쪽 위에 늘 상태가 있다** — `자동저장 꺼짐` · `저장 대기` · `저장 중…` · `저장됨 · 시각`
- **커서를 뺏지 않는다.** 저장하려고 편집을 끊으면 커서가 튀므로, 값만 조용히 옮기고 화면과 커서는 건드리지 않는다
- 크롬·엣지에서 동작한다. 그 밖의 브라우저는 「내려받기」로 떨어진다

**Esc 의 뜻이 바뀌었다.** 치는 즉시 반영되는 이상 Esc 로 「없던 일」을 만들 수 없다. Esc 는 그 칸에서 빠져나오는 것이고, 되돌리기는 Ctrl+Z 다. 한 칸을 고친 것은 글자 수와 상관없이 **한 걸음**이다 (구글 문서와 같다).

### 문서 드리프트를 기계로 (PM 지적 2026-08-29)

README 가 없는 기능을 설명하고 있었다: `1a·1b` 자동 번호(v0.23 폐기)·`why` 속성과 「↳ 이유:」(v0.25 폐기)·죽은 `#로드맵` 링크. 문서가 없는 기능을 설명하면 사람도 에이전트도 그걸 믿는다.

- **lint 7 폐기 용어 목록**에 없앤 문법을 넣었다
- **lint 12 를 「문서→문서 앵커 전부」로 넓혔다** — 죽은 링크 0
- 넣자마자 3건을 더 잡았다. 그중 하나는 **PNG·인쇄 출력이 아직 옛 모델(`why`·`subs`)을 그리고 있던 실제 버그** — 그림에서 화살표 줄과 들여쓰기가 통째로 빠지고 있었다

### 그 밖에

- **패널 폭이 화면의 비율.** 기본 절반, 3분의 1 부터 90% 까지. px 로 잡으면 27인치에서는 쪽지만 하고 노트북에서는 화면을 삼킨다. 저장도 비율로 남아 다른 컴퓨터에서도 «절반» 이다
- **링크 만들기 제거** (단추·Ctrl+K). 이미 있는 `<a href>` 는 그대로 살고, 위험한 주소는 여전히 죽는다
- **미저장 빨간 점 → 「전부 삭제」.** 안 저장됨은 아래 줄에 글자로 뜬다. 전부 삭제는 물어보고 지우며 Ctrl+Z 로 돌아온다
- **`＋` 가 옛 마크업을 찾던 버그.** 블록의 `＋` 를 눌러도 항목 제목 뒤에 줄이 생겼다
- **`edLink` 유실 버그.** 블록 개편 중 함수가 통째로 사라져 🔗 단추와 Ctrl+K 가 죽어 있었다 (e2e 가 잡았다)
- **프로토타입의 CSS 가 우리 UI 로 새던 버그.** 프로토타입에 `button{flex:1}` 같은 전역 규칙이 있으면 패널의 서식 단추가 폭 전체로 늘어났다. 라이브러리 기본값이 `:where()` 라 특정도 0 이어서 프로토타입 규칙에 지고 있었다
- **smoke 하네스.** 예제의 전체 화면 모달이 열린 채로 남아 목차 클릭을 막았다. 마커와 같이 force 로 누른다

e2e 301 → **358**, lint 26, smoke 6개 예제 전부 통과. 위계 전수(272가지)는 `--grid` 로 따로 돈다.

## v0.24.0 (2026-08-30) — 패널을 노션처럼

**기능 설명 패널의 뼈대를 다시 세웠다 (fix #52).** PM: 「편집 누르면 노션처럼 빈칸 쭉 나오고 플러스 버튼 있고 드래그할 수 있는 점 6개 보이고. 편집모드 들어간다고 밑에 쉐이드 있고 이런 거 싫어.」

- **손잡이가 블록 앞으로.** 블록마다 **왼쪽 거터**에 `＋` 와 `⠿` 가 붙고, 마우스를 올린 블록에만 나온다. 전에는 손잡이가 항목 맨 아래에 있어 무엇에 붙은 것인지 알 수 없었다
- **`＋` 와 `/` 가 같은 메뉴**를 연다. 손이 마우스에 있으면 ＋, 키보드에 있으면 `/`
- **편집 장식을 없앴다.** 노란 배경·밑줄 같은 「편집 중」 표시를 걷어냈다. 편집은 상태가 아니라 그냥 쓸 수 있는 것이어야 한다
- 번호 블록은 하나의 덩어리로 보인다 (콜아웃 느낌 — PM 확인)

**번호가 진짜 «블록» 이 됐다 (PM QA 2026-08-30).** 콜아웃으로 하기로 했는데 번호 블록이 여전히 「표의 한 행」 모양이었다. 구분선과 왼쪽 번호칸을 걷어내고 **둥근 카드 + 번호 배지**로 바꿨다. 크게 보면 에디터 안에 번호 블록이 있고, 그 안에 줄이 들어가는 구조다.

- **줄마다 붙던 「지우기」 버튼 제거** — 빈 줄에서 Backspace 가 그 일을 한다. 버튼이 하나 더 있으면 「어느 쪽이 맞나」를 사용자가 판단해야 한다
- **항목 삭제를 제목 줄 오른쪽으로** (옛 유형 라벨 자리). 블록 머리에 있어야 「이 덩어리를 지운다」로 읽힌다
- **줄 하나 지운 뒤 Enter 를 치면 남은 줄까지 사라지던 버그를 고쳤다.** 화면을 다시 그리면 옛 입력칸이 문서에서 떨어져 나가는데, 그것을 뒤늦게 저장하면 번호가 이제 다른 줄을 가리킨다. **떨어져 나간 칸의 편집은 이미 지난 일**이므로 무시한다

**패널 폭을 좌우로 끌어 조절 (fix #53)** — 왼쪽 가장자리를 잡아 끈다. 320~720px, 고른 폭은 그 사람 브라우저에 남아 다음에 열어도 그대로다. 폭이 바뀌면 시트 배율이 달라지므로 마커를 다시 놓는다. 키보드로도 된다(손잡이에 초점 + 좌우 화살표).

**lint 22: CSS 중괄호 균형** — 이번에 CSS 한 줄이 잘못 지워져 닫는 괄호가 사라졌고, **그 뒤 규칙 전부가 조용히 죽어** 패널이 스크롤되지 않았다. 화면은 그럴듯해 보여서 눈으로는 못 잡는다. 기계가 센다.

e2e 292 → **301**.

## v0.23.0 (2026-08-30) — 번호는 숫자만

**하위 요소(1a·1b)를 없앴다 (fix #51).** PM: 「1a 1b 이거는 너무 규격화되지 않은 것 같아. 없애고 그냥 숫자만 가자.」

- `parts` 로 «1a»·«1b» 를 매기던 규칙을 제품에서 뺐다. **1a 로 쓸 것은 새 번호로 전부 되고**, 깊이가 필요하면 그 번호 안의 불릿(3단까지)이 한다
- 옛 문서에 `parts` 가 남아 있어도 **조용히 무시한다.** 열자마자 깨지면 그건 없앤 게 아니라 부순 것이다
- 예제(shop·floating)의 하위 요소를 번호·줄로 옮겼다. 오른쪽 유형 라벨도 함께 제거(#50)
- `tests/lint.js` 의 Part 커버리지 검사(17번)도 지웠다 — 없는 개념을 지키는 검사가 남아 있으면 다음 사람이 되살릴 이유를 찾는다

e2e 294 → 292 (하위 요소 전용 검사를 «없어졌는지» 검사로 교체).

## v0.22.0 (2026-08-30) — 에디터 개편

**정의서를 쓰는 방식이 바뀐다.** 버튼으로 넣고 빼던 편집을 걷어내고, 쓰고 지우는 편집으로. 기획에만 집중한다.

**슬래시는 셋뿐 (fix #42)** — 번호 · 불릿 · 이유. 「설명 줄 / 하위 줄」로 나눴던 것은 우리 데이터 구조(`defs`·`subs`)를 그대로 메뉴로 내보낸 실수였다. 둘은 같은 불릿이고 **층은 Tab 이 정한다.** 오른쪽에는 노션처럼 마크다운 단축키(`-` 불릿 · `?` 이유) — 슬래시를 안 열고도 바로 친다. 메뉴가 떠 있을 때 ↑↓ 로 고르고 Enter 로 넣는다.

**번호를 화면에서 찍는다 (fix #43)** — 지금까지 번호는 프로토타입에 미리 심어 둔 `data-spec` 이 있어야만 붙었다. 기획자가 쓸 수 있는 길이 아니었다.

- `/` → 「번호」 → 화면이 찍기 모드. 마우스를 올리면 그 요소가 잡히고 이름표(무엇 · 크기)가 뜬다
- **↑↓ 로 부모·자식, ←→ 로 형제.** 클릭 한 번으로 원하는 것을 정확히 잡는 건 거의 불가능하다(작은 글자가 잡힌다) — 개발자도구·피그마가 쓰는 관습을 그대로 빌렸다. 배울 것이 없다는 게 이 방식의 값이다
- 클릭하면 번호가 붙고 **패널의 그 줄로 커서가 들어간다.** Esc 로 취소. 이미 번호가 있는 곳을 찍으면 새로 만들지 않고 그 항목으로 간다
- 찍기는 프로토타입에 이름표를 **직접 쓴다**. 우리 약속은 「코드 불변」이 아니라 **「동작 불변」**이다 — 보이지 않는 이름표라 프로토타입은 하던 대로 움직인다 (로드맵 D7)

**손잡이 대청소 (fix #45)** — `＋ 줄` · `＋ 하위 줄` · `＋ 이유` · 줄마다 붙던 `×` 를 전부 걷어냈다. 넣기는 Enter·Tab·슬래시가, 지우기는 빈 줄 Backspace 가 한다. 마우스를 쓰는 사람을 위해 **올렸을 때만** 나타나는 `⋮` 하나만 남겼다 — 마우스를 올리지 않으면 정의서가 읽기 화면과 같아 보인다.

**아카이브 (fix #46)** — 개발 정의 · 유형 8종 드롭다운 · 하위 요소(1a·1b), 셋의 **「만드는 길」만** 없앴다. **렌더는 그대로**라 기존 문서는 한 픽셀도 안 바뀐다.

- 개발 정의: 흐름상 개발은 개발 탭에 적는 것이 맞다. 기획을 먼저 완성시킨다
- 유형 8종: 골라도 화면이 거의 안 바뀌었다 — 실제 동작은 4가지고 그중 넷(영역·입력·상태·모션)은 라벨만 다르다
- 하위 요소: 사용자 개념이 아니라 데이터 개념이다. `1a` 로 쓸 것은 새 번호로 전부 된다
- 되살리는 조건: 외부 요청 2건 이상

**굵게와 링크 (fix #44)** — 기획자가 꼭 필요하다고 한 둘만 넣었다. `Ctrl+B` · `Ctrl+K`.

- 저장 형식을 **컨플루언스 XHTML 의 최소 부분집합**으로 못박았다: `<strong>` 과 `<a href>` 뿐. 그래서 컨플루언스로 내보낼 때 **변환기가 아예 없다** — 태그를 그대로 실어 보낸다. 형태가 다른 곳(노션)으로 갈 때만 얇은 변환기 하나가 필요하다
- 브라우저는 굵게를 `<b>` 로 만든다. **저장 입구에서 한 번만** `<strong>` 으로 바꾸면 그 뒤로는 어디로 내보내든 손댈 것이 없다. 화면도 저장된 형태로 즉시 맞춘다 — 보이는 것과 저장된 것이 갈라지면 다음 편집이 어긋난 채 시작된다
- 기울임·밑줄·색은 붙여넣어도 **글자만 남는다.** 허용 목록 밖은 애초에 만들 수 없다 — 서식을 늘리고 싶은 유혹을 이 한 줄이 막는다
- 링크 주소는 `http(s)` 와 `mailto` 만. `javascript:` 는 거절한다

**용량 상한 60 → 150KB (fix #47)** — 다만 이 예산은 **엔진 수입이 아니라 우리 코드에 쓴다.** Editor.js(gzip 64KB)가 유일한 무빌드 후보였지만, 범위가 「슬래시 3종 + 볼드·링크」로 줄면서 우리 에디터가 이미 9할을 하고 번호는 어차피 커스텀이다. Tiptap·Lexical 은 npm 번들링이 전제라 「파일 하나 복사」라는 제품 성격이 깨진다. 지금 59.3KB.

e2e 266 → **284**.

## v0.21.0 추가 (2026-08-29, 같은 판에 포함)

**내보낸 그림의 밑단이 딱 떨어진다 (fix #41).** PM 실사용(컨플루언스 발행)에서 그림 아래 넓은 흰 띠가 발견됐다. 원인은 셋 — `sticky` 는 스크롤 없는 전체 높이 그림에서 기준을 잃고, `backdrop-filter` 요소는 SVG 캡처에서 통째로 안 그려지고, `aspect-ratio` 요소는 납작해져 아래 내용이 전부 위로 당겨진다. 캡처 순간 셋을 중화하고(라이브 화면은 원상 복구), 그 위에 안전망 한 줄: **밑단은 잉크가 끝난 곳에서 자른다** — 앞으로 어떤 CSS 가 SVG 에서 줄어들든 아래 띠는 구조적으로 안 남는다. 머리말은 ID·화면명·경로만(일시 제거), Made with 는 그림 맨 아래 오른쪽에 캔버스로 직접 쓴다.

## v0.21.0 (2026-08-28)

**정의서를 글 쓰듯 고친다 (fix #40 · Phase 0 의 0-6).** 지금까지 편집은 「불렛 자리를 우리가 잡아 주고 버튼으로 넣고 뺀다」였다. 이제 노션에 글 쓰듯 친다 — 다만 **«최소한의 노션»** 이다.

- **Enter = 새 줄** (전에는 「반영」). 고친 것은 치는 즉시 반영되고, 파일로 남기는 것은 「저장」이다
- **Tab / Shift+Tab** 으로 한 단 들어가고 나온다 · **빈 줄에서 Backspace** 면 그 줄이 사라진다
- **`/`** 로 넣을 것을 고른다: 설명 줄 · 하위 줄 · 이유 · 개발 줄
- **유형 드롭다운** — 항목 제목 옆에서 `anno` 8종을 바꾼다. 새 필드가 아니라 이미 있는 값이다
- **Ctrl+Z / Ctrl+Shift+Z 여러 걸음.** 설정 전체를 찍어 두고 제자리로 되돌리는 방식이라 편집 이력을 따로 들지 않는다 — 되돌릴 수 없는 편집 동작이 생길 여지가 없다
- **하위 항목이 3단까지.** `subs` 의 원소가 글자면 2단, `{t, subs}` 면 그 아래 한 단 더. **번호는 2단(1a)까지만** 붙이고 3단은 글머리표다 — 화면 위 마커가 1·1a 까지만 찍히므로 번호가 더 깊어지면 어긋난다
- **빈 줄은 남지 않는다.** 만들고 아무것도 안 쓰면 스스로 사라진다
- **에디터 엔진을 넣지 않았다.** 우리 데이터가 이미 블록 목록이라 스키마에 항목을 꽂는 UI 면 충분하다. ProseMirror 코어 ~40KB · Quill ~43KB 로 하나만 넣어도 라이브러리가 두 배가 된다 (지금 54.9KB / 상한 60KB)
- 하위 호환: `subs: string[]` 만 쓰던 문서는 한 글자도 안 바꿔도 되고, 편집을 끄면 정의서 DOM 이 편집 기능 없던 때와 같다

**움직이는 요소 안의 마커가 따라간다 (fix #8).** 캐러셀처럼 `transform` 으로 미끄러지는 요소 **안쪽**에 마커를 달면 번호만 제자리에 남았다. 마커를 다시 놓는 계기가 「창 크기 변경·DOM 변경」뿐이었기 때문이다. 대표 데모(쇼핑 홈)에서 실제로 보이던 증상이다.

- 전환·애니메이션이 시작되면 **그동안만** 따라가고, 더 움직이지 않으면 스스로 멎는다. 상시 루프는 배터리를 태운다
- 멈추는 조건을 「시작 N번·끝 N번」 이벤트 짝맞추기로 세지 않는다 — 요소가 도중에 사라지면 끝 이벤트가 오지 않아 루프가 영원히 남는다. **움직였는지를 매 프레임 실제로 보는 편이 스스로 낫는다**
- 마커와 무관한 장식 애니메이션(스피너 등)은 따라다니지 않는다 — 대상을 품은 요소만 고른다
- 마커 좌표는 **값이 바뀔 때만** 쓴다. 스타일 쓰기는 그 자체로 DOM 변경 신호라 감시자들을 깨운다
- 한계: CSS 전환·애니메이션 없이 JS 가 매 프레임 좌표를 바꾸는 방식은 여전히 신호가 없다. 그때는 마커를 바깥 컨테이너에

**보드가 작업을 따라온다.** 푸시하면 커밋 메시지의 `fix #N` 을 읽어 그 카드를 완료로 보낸다(`.githooks/pre-push`). 훅은 저장소에 따라오지 않으므로 `scripts/backlog-sync.js` 가 스스로 켠다.

## v0.20.0 (2026-08-27)

**정의서를 읽는 것에서 쓰는 것으로.** 지금까지 ScreenSpec 은 AI 가 붙인 정의를 «보여주는» 도구였다. 이번 판부터는 받은 초안을 **사람이 직접 고치고, 뽑아서 넘기고, 개발 정의까지 같은 자리에 쌓는다.** 네 가지가 한 묶음이다.

**편집 모드 (#37)** — 정의서 패널의 「편집」 을 켜면 글자를 눌러 그 자리에서 고친다. `window.SCREENSPEC` 이라는 JS 객체를 열 일이 없다.

- 항목명·설명 줄·이유·하위 줄 수정 (**Enter** 반영 · **Esc** 취소), 줄 추가·삭제, 이유 붙이기, 순서 바꾸기(↑↓), 항목·세부 삭제
- 순서를 바꾸거나 지우면 마커 번호를 1부터 다시 매긴다 — 번호가 비면 읽는 사람이 「빠졌나」를 의심한다
- 저장은 세 경로를 겹친다: **파일에 직접**(파일 접근 API) · **내려받기**(`이름.edited.html`) · **설정 복사**(원본에 붙여넣거나 AI 에게 「이걸로 교체해줘」)
- 어느 경로든 **저장 안 된 초안**이 브라우저에 깔린다. 저장 없이 닫았다 열면 「이어서 / 버리기」를 묻는다
- 저장은 원본 HTML 의 **설정 블록만** 갈아끼운다 — 프로토타입 코드는 바이트 그대로 남는다. 다만 설정 블록 안의 **주석은 사라진다**
- `readonly: true` 면 편집 버튼도 저장 경로도 **만들지 않는다**(숨김이 아니라 미생성) — 넘겨줄 사본용

**내보내기 — PNG (#34)** — 툴바의 「내보내기」로 화면을 컨플루언스·노션에 붙일 수 있게 뽑는다. **PNG 하나로 통일했다** — PDF/인쇄는 넣지 않는다.

- **화면 전체 높이로 뽑는다.** 기기 화면(360×800)보다 내용이 길면 스크롤해야 보이는 아래쪽까지 한 장에 담는다 — 기기 높이만 자르면 아래쪽 마커가 통째로 사라진다(`examples/demo.html` 은 마커 10개 중 7개가 날아갔다)
- **세 모드 전부** — `wrap` 은 살아 있는 시트를 잠깐 옮겼다 놓고, `overlay` 는 본문 사본을 떠서 뷰어 UI 만 걷어내고, `frame` 은 same-origin 을 이용해 액자 안 문서를 직접 뜬다
- 선택은 셋: **번호 표시 · 머리말 표시 · 기능 설명 포함**(기본 꺼짐). 레이어는 「기능 설명 포함」을 켜야 활성화된다 — 표가 없으면 거를 것이 없다
- **머리말을 그림에 굽는 이유**: 붙여 넣은 그림은 즉시 낡는다 — 항목 순서를 바꾸면 번호가 다시 매겨지는데 컨플에 붙인 그림은 그대로다. 화면 ID·생성 일시가 박혀 있어야 어느 판인지 가릴 수 있다
- **여백은 마커가 실제로 튀어나온 만큼만** 잰다. 고정 여백은 그림 둘레에 쓸데없는 흰 띠를 남긴다
- **캡처 라이브러리를 넣지 않았다.** DOM→SVG→canvas 로 브라우저 기본 기능만 쓴다(2배 해상도, 100~240ms). `html2canvas` 하나가 이 라이브러리 전체보다 크다
- 못 하는 것은 **조용히 넘기지 않는다**: 바깥 주소 이미지는 빈칸으로 나오므로 **몇 개인지 알려 주고**, 그림이 비어 나오면 내보내지 않는다
- **버튼이 툴바로 갔다.** 내보내기는 설명 패널이 아니라 **화면 전체**에 작용하는 동작이라 「기능 설명」 머리에 있던 것은 위계 오류였다

**개발 정의 레이어 (#38)** — 개발 PM 이 같은 항목 옆에 정책·API·이벤트를 적는다.

- 줄 단위 `layer: "dev"` (한 항목 안에 기획·개발이 섞이는 게 실무의 모습이다) · 항목에 안 붙는 화면 공통 정책은 `screen.dev`
- 탭으로 가르지 않는다. 개발 정의는 기획 정의를 **보면서** 쓰는 글이라 같은 항목 안에 들여쓴 블록으로 붙고 `DEV` 태그가 달린다
- 설명 패널에 레이어 칩 **전체 | 기획 | 개발**. 리뷰어는 기획만, 개발자는 개발만 본다. 인쇄도 같은 축을 쓴다
- 필터는 **보기만 바꾼다** — 마커 개수·누락 경고·상태 커버리지에 영향이 없고, 행 자체도 숨기지 않는다(번호와 마커의 대응 유지)
- **어느 층에서든 기획이 먼저, 개발이 나중.** 화면 공통 개발 정의도 항목들 뒤에 온다 — 층마다 순서가 다르면 규칙을 한 문장으로 설명할 수 없다

**온보딩 인터뷰 · `style` (#36)** — AI 가 쓰기 전에 「이 프로젝트는 어떻게 쓰는가」를 먼저 묻는다.

- SKILL.md §0 에 네 문항(문구 어휘 · 상태 축 · 화면 ID 체계 · 기준 폭). 이미 대화에 답이 있으면 건너뛰고 「전부 기본으로」로 끝난다
- 답은 `style` 에 남아 다음에 다시 쓸 때도 톤이 유지된다. **`style` 은 AI 가 읽는 계약이라 라이브러리 렌더는 바뀌지 않는다** — 형식만 보고 어긋나면 어느 항목인지 짚어 1회 경고한다

**하위 호환** — `layer`·`dev`·`style`·`readonly` 를 안 쓰던 문서는 화면이 예전과 완전히 같다. 편집 손잡이는 편집을 켜야만 렌더되고, 레이어 칩은 개발 정의가 있어야만 생긴다.

**공개 API 추가** — `exportImage({markers,head,table,layer,scale})` · `edit(bool)` · `serialize()` · `dirty()`

**호환** — `@0` 으로 쓰던 문서는 그대로 동작한다. 전달본을 고정하는 세 방법은 [README](README.md)에 적었다.

e2e 150 → **247 케이스**. 내보내기는 세 모드 전부에서 실제로 PNG 를 만들어 **백지가 아닌지(잉크 비율)·전체 높이인지·원상 복귀하는지** 까지 확인한다. lint 에 **gzip 60KB 상한**을 넣었다.

## v0.19.3 (2026-08-26)

**비동기 조회 화면의 누락 오탐** — #23 이 고쳤다고 한 문제가 실제로는 동작하지 않고 있었다. 데이터가 늦게 오는 화면에서 아직 도착하지 않은 요소까지 「못 찾은 정의」로 경고했다.

- 원인 ①: 앱을 감싸는 컨테이너(`.ss-proto-wrap`·`.ss-docmode`)에 `ss-ui` 가 붙어 있어, 감시자가 **앱의 변경을 우리 UI 변경으로 오인해 전부 무시**했다. 1.5초 조용 타이머의 리셋이 한 번도 발화하지 않아, 데이터 도착과 무관하게 **부팅 1.5초 뒤 무조건 판정**했다 (주입을 0.7·1.5·2.5초로 늦춰도 판정은 항상 ~1.52초로 고정되는 것을 실측)
- 원인 ②: 조용해진 첫 판정에서 곧바로 경고했다. **조용하다고 다 온 것은 아니다** — 응답이 1.5초보다 늦는 화면은 그 사이 DOM 이 조용하다
- 조치: 앱 변경 판별을 `.ss-holder` 기준으로 고쳐 리셋을 되살리고(마커 재배치 등 우리가 «그린» 것은 계속 제외), **전부 찾으면 즉시 종료(clean) · 못 찾은 게 있으면 상한(5초)까지 기다렸다 경고**하도록 바꿨다. 낙관적으로 기다린다
- 경고가 늦어지는 것은 **실제로 누락이 있을 때뿐**이다. 다 찾은 화면은 예전처럼 조용히 끝나고 아무것도 출력하지 않는다

e2e: 비동기 케이스를 0.7초·2.5초 두 가지로 넓히고, 고정 대기를 «경고가 도착할 때까지» 조건 대기로 바꿨다(경합 제거 — 여유 51ms 로 간헐 실패하던 케이스였다). 150 케이스.

## v0.19.2 (2026-08-25)

**기기 화면이 곧 뷰포트다** — wrap·frame 모드에서 프로토타입의 `position:fixed` 요소(플로팅 버튼·바텀시트·전면 모달·고정 앱바)가 폰 화면을 탈출해 브라우저 창에 붙던 문제.

- 증상: 프로토타입 모드에서 FAB 이 폰 옆 허공에 뜨고, z-index 가 큰 전면 모달은 우리 툴바까지 덮었다. AI 가 만드는 프로토타입은 이런 고정 요소를 거의 항상 쓰므로 **파일마다 깨지고 안 깨지고가 갈렸다**
- 원인: `position:fixed` 의 기준은 기본적으로 브라우저 창이다. 정의서 모드는 축소 `scale()` 이 걸려 우연히 갇혀 있었고, **프로토타입 모드만 새고 있었다** — 같은 파일이 모드에 따라 다르게 보이던 이유
- 조치: 기기 시트(`.ss-frame`)를 상시 기준 상자로 만든다. 두 모드·두 폭에서 규칙이 같아지고, 전면 모달은 창이 아니라 **폰 화면을 덮는다**(실제 기기와 같은 거동). 본문을 스크롤해도 고정 요소는 화면에 붙어 있다
- overlay 모드는 앱과 뷰어가 한 창에 사는 구조라 이 방법이 통하지 않는다 — 폭 확인·겹침 회피가 필요하면 `mode:"frame"` (기존 안내와 동일)

**릴리스 스크립트** `scripts/release.js` — 태그 → 푸시 → jsDelivr 퍼지 → 실물 확인을 한 번에. 버전 문자열만 올리고 태그를 안 만들면 README·SKILL 이 404 를 가리키게 되는데, lint 는 «서로 같은지» 만 보고 «실재하는지» 는 못 본다(의존성 0 정적 검사). 그 구멍을 막는다.

예제 `examples/floating.html` 추가 — 앱바·필터·FAB·탭바·전면 시트를 한 화면에 모은 고정 요소 회귀 대상. e2e 에 두 모드 × 두 폭 가둠 검사와 스크롤 고정 검사 추가.

## v0.19.1 (2026-08-24)

문서·예제 정비 (라이브러리 동작 변화 없음).

- **README 재배치** — 「만든 뒤 — 전달과 공개」를 모드·문법 소개 **뒤**로 (처음 읽는 사람이 `screens`도 모른 채 off·inline 을 만나던 순서 문제). 자주 막히는 곳 13행 → 10행(증상 중복 병합)
- **preview 를 README 에** — 특징 불릿 + 실무 문법 표. v0.18 대표 기능인데 첫 화면에 0회 등장했다
- **대표 데모(shop)가 v0.16~v0.18 문법을 실제로 시연** — `checklist`+`covers`/`skip`(홈은 완비, 상세는 ⚠ 미정의 배지), 「딜 마감 상태 보기」 preview 스위치(앱 리스너 포함). 빠른 시작 예제에 `why` 한 줄
- 스크린샷 3장 v0.19 UI 로 재촬영 (기존 v0.11 시점) · AGENTS 릴리스 절차에 smoke·`gh release create` · SKILL 의 폐기 기호 ◑ 문구 정리
- lint: 폐기 어휘 추가(`panel:"left"`·「상위 N · 하위 M」·「패널 ⇄」) · `### Part` 검사 CRLF 체크아웃 대응

## v0.19.0 (2026-08-24)

v0.18 을 실제로 붙여 본 리포트 3건(#29·#30·#31) — 전부 «동작은 맞는데 뜻이 안 읽힌다» 계열이다.

**preview 가 스위치로 보인다** (#29)
- 켜면 라벨이 **「원래대로」** 로 바뀐다 — 되돌리는 방법이 그 자리에 있다. 「눌러서 목록이 비었는데 어떻게 되돌리지」 가 사라진다
- `◑` 를 버리고 **토글 스위치 모양**으로. ▶(실행)과 다른 물건임이 모양으로 읽힌다
- 켜져 있는 동안 앱 위에 띠 한 줄 — `◑ 「빈 상태」 재현 중 — 실제 데이터가 아닙니다  [끄기]`. 화면만 보는 사람(디자이너·개발자)도 가짜 상태임을 안다. 라이브러리가 그리므로 앱마다 모양이 갈리지 않는다. 띠가 뜨면 그 높이만큼 아래를 밀어 정의서 헤더를 덮지 않는다
- 「현재 미표시」 배지를 눌러도 켜진다 — 「지금 화면에 없음 → 눌러서 보기」가 한 흐름이 된다

**항목 수 문구** (#30) — `상위 11 · 하위 4` → **`항목 11개 · 세부 4개`**. 「상위·하위」는 관계를 가리키는 말이라 기준점을 모르면 읽히지 않는다. 세부가 없으면 `항목 11개`로 도입 전과 같다

**기획 문구 뼈대** (#31) — v0.16 규칙이 *하지 말 것* 만 잡아 *할 것* 이 비어 있다는 실사용 회고. SKILL §4 에 접두사 어휘(`기본값 :` `선택값 :` `클릭 :` `정렬 :` `표시문구 :`)·종결 어휘(`~가능`/`~불가`)·항목 뼈대·자주 나오는 실수를, §4-B 에 **화면 유형별로 채울 것**(목록·폼·상세 패널·자리 화면)을 추가. 접두사는 강제하지 않고 기본 한 벌만 제시하며 «섞지 말 것» 만 규칙이다

e2e 119 → 134.

## v0.18.1 (2026-08-24)

**상태 점검(⚠) 표시가 스스로를 설명한다** — 이 기능을 설정하지 않은 사람이 보면 ⚠ 가 무슨 뜻인지 알 수 없었다.

- 카드 문구를 「⚠ 이 화면에 「로딩 · 오류」 설명이 없습니다」 + 「이 프로젝트가 화면마다 챙기기로 한 항목입니다. 설명을 더하거나, 이 화면에 해당 없으면 사유와 함께 「해당 없음」으로 적으면 사라집니다」 로 — **무엇이 빠졌고 뭘 하면 사라지는지**를 카드 안에서 말한다
- 마우스를 올리면 어느 설정에서 온 것인지(`checklist`)와 고치는 방법(`covers`·`skip`)이 뜬다. 목차 배지도 같은 설명
- `checklist`가 켜져 있으면 부팅 때 콘솔에 한 줄 — 설정을 직접 넣지 않은 사람도 출처를 알 수 있게
- SKILL: `checklist`를 넣었으면 **그게 무엇인지 사용자에게 한 줄로 알리도록** 규칙화. 「이 ⚠ 뭐냐 / 없애줘」 요청을 받았을 때의 처리(정의 추가 + `covers`, 또는 사유와 함께 `skip`)도 명시

## v0.18.0 (2026-08-24)

**상태를 화면에서 재현 — `preview`** (#27) — 빈 상태·로딩·오류는 지금 화면에 없어서 글로만 읽을 수 있었다. 이제 정의서에서 눌러 실물을 볼 수 있다.

```js
{ n: 9, anno: "state", title: "목록 공백 상태", preview: { label: "빈 상태 보기" }, defs: [...] }
```

- 누르면 정의서가 `screenspec:preview` 이벤트를 **앱의 창**(frame 모드면 액자 안)으로 쏘고, 앱이 그 상태를 만든다. 라이브러리는 앱의 상태 관리 방식을 알 필요가 없다
- **앱이 받았는지 확인한다** — 리스너가 `e.detail.handled = true`로 응답하면 버튼이 켜진 상태로 남고, 아무도 안 들으면 「이 프로토타입은 아직 이 상태를 만들지 못합니다」를 그 자리에 적는다. 죽은 버튼이 아니라 *문서에는 있는데 실물이 없다*는 정보가 된다
- 동시에 하나만 켜지고, 화면을 옮기면 자동으로 꺼진다 (앱이 가짜 상태에 갇히지 않게)
- 하위 요소(`parts`)에도 붙는다

**상태 커버리지 표시를 다시 썼다** — 「상태 커버리지 — 전부 다룸 (3개 축)」처럼 늘 떠 있어 무엇을 하라는 것인지 애매했다. 이건 정의서를 *쓰는 사람* 을 위한 체크리스트이므로 **다 채우면 사라진다**. 빠진 것이 있을 때만 점선 카드로 `⚠ 아직 적지 않은 상황 — 로딩 · 오류`. 「축·다룸·비움」 같은 용어도 문장으로 바꿨다.

e2e 109 → 119.

## v0.17.1 (2026-08-24)

문서·검사만 손봤다 (라이브러리 동작 변화 없음).

- **`Part.optional` 문서화** (#28) — 코드는 v0.16.0부터 동작하는데 레퍼런스의 `Part` 타입에만 빠져 있었다. 팝업·패널 안의 하위 요소는 닫혀 있는 것이 정상이라 `optional: true`가 사실상 필수인데, 문서에 없으면 경고를 보고 `target`을 지우게 된다
- **재발 방지 검사 추가** — `Part` 타입·표가 `Spec` 필드(−`n`·`parts`)를 전부 갖는지 lint가 검사한다. 하위 요소는 같은 파이프라인을 쓰므로 필드가 같고, 앞으로 `Spec`에 필드를 더하면 `Part` 문서도 같이 고쳐야 통과한다 (음성 테스트로 검증)
- **README 구조 정리** — 「빠른 시작」에 섞여 있던 전달·끄기를 「만든 뒤 — 전달과 공개」로 묶고, 흩어져 있던 anno 표·실무 문법 표·레퍼런스 안내를 「무엇을 적을 수 있나」 한 절로 합쳤다 (13절 → 12절)

## v0.17.0 (2026-08-24)

**정의서 공개 스위치 `off`** — 정의서를 *붙이는 것*과 *공개하는 것*을 분리했다. 초기 리뷰·외부 데모에서 프로토타입만 보여주려고 정의를 뜯어냈다 다시 붙이는 낭비를 없앤다.

```js
window.SCREENSPEC = { off: true, screens: [ /* 정의는 그대로 */ ] };
```

- `off: true` → 라이브러리가 **아무것도 하지 않는다**. 모드 토글·마커·설명 패널·주입 CSS·DOM 변경 전부 없이 원본 프로토타입 그대로
- **주소가 설정보다 강하다** — `off: true`로 배포해 두고 리뷰할 사람만 `?screenspec=1`(또는 `#screenspec`)로 열면 된다. 파일 하나로 두 청중. 반대로 `?screenspec=0`은 설정을 안 고치고 그 탭에서만 끈다
- off 에서도 `setScreen()`·`refresh()`는 빈 함수로 남는다 — 프로토타입이 그 호출을 갖고 있어도 깨지지 않는다. `mode`는 `"off"`
- 조용히 실패하지 않는다 — 켜는 방법을 `console.info` 한 줄로 알린다(화면에는 아무것도 뜨지 않음)
- **숨김이지 보안이 아니다**: 정의 텍스트는 페이지 소스에 남는다. 문서·SKILL 에 명시

e2e 101 → 109.

## v0.16.0 (2026-08-24)

정의서의 **구조**를 실무 화면정의서 문법에 맞춤 (화면 18장 실측 회고 #24~#26 반영).

- **`parts` — 영역 안의 이름 있는 하위 요소** (#25). 라벨 `1a·1b` 는 라이브러리가 자동으로 매김. 하위 요소도 자기 마커·anno·▶ 재생·조건(`subs`)·이유(`why`)를 가짐. 헤더 항목 수는 「상위 N · 하위 M」
- **`checklist` — 화면별 상태 커버리지** (#26). 프로젝트가 축(빈 상태·로딩·오류…)을 정하면 화면마다 `covers`/`skip`(사유 필수)으로 다룬 것과 일부러 비운 것을 선언. 목차에 「⚠ 로딩 · 오류 미정의」 배지, 패널 하단에 커버리지 블록. 선언 없이 자동 추론하지 않음
- **`why` 칸 + 문구 규칙 교체** (#24). 근거는 본문에 대시로 붙이지 말고 `why` 에 — 패널에서 「↳ 이유:」로 분리. SKILL §4 규칙 5·6: 이유는 `why` 에, 디자인 값·구현 방식(px·중단점·줄바꿈/스크롤)은 적지 않는다. 팀별 `style` 설정은 채택하지 않음
- **`optional: true`** — anno 와 무관하게 조건부 요소. 누락 경고에서 제외 (#23 후속)
- **위치 힌트 자동** — 마커 실제 좌표에서 「상단 · 전체폭」「하단 · 우측」을 계산해 제목 옆에. 화면 없이 패널만 읽어도 어디인지 알 수 있게 (실무 정의서의 「상단 타이틀 영역」 관례를 사람이 적지 않고)
- **설명 패널 오른쪽 고정** — v0.14 의 `panel:"left"`·헤더 「패널 ⇄」 제거(설정은 폐기 경고). 겹침의 정식 해법은 v0.15 의 `mode:"frame"`

e2e 84 → 101.

## v0.15.0 (2026-08-24)

**frame(액자) 모드** (#17 · #14 의 공통 뿌리) — 프레임워크 앱을 iframe 에 넣고 뷰어(툴바·모바일/PC 폭·설명 패널·마커·목차)는 바깥에 둔다. `mode:"frame"` 을 명시해야만 켜짐.
- 설명 패널이 앱을 덮지 않는다 (우측 서랍 문제 소멸) · 툴바의 모바일/PC 로 앱의 미디어쿼리가 실제로 발화
- 액자 안 라우팅·스크롤·DOM 변경을 바깥에서 추적, 바깥 주소도 따라감(새로고침해도 같은 화면). 목차·flow·▶ 재생은 액자 안 앱을 조작
- 조건: 앱이 주소로 열리고 same-origin. 액자 안에 로드된 사본은 UI 를 만들지 않음
- overlay 의 화면 감지를 공용 함수로 추출, 기존 wrap·overlay 동작은 불변

**반응형을 문서에 남기기** (#17 코멘트)
- `baseViewport: "pc" | "mobile"` — 이 문서가 서술하는 기준 폭 = wrap·frame 시작 폭 (어드민은 PC)
- 화면 메타 `viewports: ["pc"]` → 목차에 「PC 전용」 배지
- SKILL: 반응형 차이는 화면을 늘리지 말고 같은 화면의 `state` 로 적는다 · §7 에 빈 상태·로딩·오류 커버리지 질문 추가 (#26 의 문서 부분)

**정의서 패널** — 지금 화면에 없는 정의(조건부 상태 등)는 번호를 흐리게 + 「현재 미표시」 (#27 의 1번)

e2e 71 → 84.

## v0.14.1 (2026-08-24)
- **누락 경고 판정을 시간이 아니라 「앱이 다 그려졌는가」로** (#23) — v0.14.0 의 400ms 고정 지연은 스켈레톤 → 본문처럼 데이터를 받아 그리는 화면을 못 덮었다. 이제 앱 DOM(ScreenSpec UI 제외)에 변경이 1.5초 동안 없을 때 세고, 계속 변하면 5초 상한. 전부 찾으면 그 화면은 종료, 못 찾은 게 있으면 그때 1회 경고

## v0.14.0 (2026-08-24)

Next.js 16 어드민 프로토타입(화면 18장·정의 96항목) 실측 리포트(#12~#21) + 자체 백로그 반영. 이슈 1건 = 커밋 1건.

**결함 수정**
- **root형 화면이 overlay 에서 활성화되지 않던 문제** (#12, #7) — 라우트가 하나라도 있으면 `root` 분기에 도달하지 못했다. 이제 「보이는 root 화면 > 라우트 > 미정의」 순으로 감지하고, 열린 패널·다이얼로그가 라우트 화면을 이긴다. `setScreen()` 은 wrap 에서 root 표시/숨김을 함께 전환(목차 클릭과 동일 로직), overlay 는 root 가 보이는 동안 유지
- **라우트 매칭 구체 경로 우선** (#15) — `/members/[id]` 를 먼저 선언해도 `/members/invite` 가 가려지지 않는다 (동적 세그먼트가 적은 순으로 매칭, 선언 순서 무관)
- **overlay 마커 클램프** (#13) — 대상이 x:0 이면 마커가 10px 잘리고, y:48 이면 정의서 헤더 밑으로 들어가던 문제
- **설명 패널 좌/우 전환** (#14) — 앱의 우측 드로어·사이드시트가 패널에 가려져 ▶ 결과를 못 보던 문제. 설정 `panel:"left"` 또는 헤더의 「패널 ⇄」 버튼. 하단 시트(≤900px)에서는 무효

**개선**
- 누락 정의 경고에 `#n target="…"` 나열, `anno:"state"`(조건부 표시)는 건수에서 제외하고 따로 표기 (#20)
- 등록됐지만 `specs` 가 빈 화면: 백지 대신 다음 할 일 안내 + 현재 화면의 `data-spec` 요소 수 (#19)
- `accent` 가 CSS 변수 `"var(--토큰)"` 을 받는다 — 제품 디자인 토큰을 복사하지 않고 가리킴(색 하드코딩 lint·다크 모드 대응). SVG 화살표 색은 속성 대신 style 로 지정 (#18)
- overlay 헤더에 앱 영역 폭(px) 표시 + `body` 에 `.ss-pc`/`.ss-narrow` 훅 + 개발자 도구 기기 툴바 안내 (#17 최소안 — iframe 시뮬레이터는 별도 설계)
- 화면 목록 검색: 화면 8개 이상이면 이름·ID 부분 일치 필터, 매칭 행과 조상만 표시 (#9)

- 목차·flow 로 화면을 옮길 때 앱이 그려지기 전에 「못 찾은 정의」 경고가 잘못 뜨던 문제 — 판정을 400ms 뒤로 미룸 (사이클 smoke 에서 발견, v0.13 부터 있던 현상)

**문서**
- overlay 에서도 `root` 가 `data-spec` 조회 범위를 좁힌다는 점을 SKILL §3-B·config.md 에 명시 (#21)

**저장소**
- `.agents/workflows/` 이슈 사이클 프로토콜(plan→dev→qa→commit, tasks.json 게이트) + `/ss-cycle`·`/ss-retro`. 사이클 기록은 `docs/sprint/2026-08-24-cycle/`

e2e 49 → 69건 + `tests/smoke.js`(예제 전수 클릭) 신설.

## v0.13.1 (2026-08-24)
- **전달본 = 자체 완결 파일이 기본.** 원본은 CDN 한 줄(설계도), 넘기는 파일은 `.inline.html`(인쇄물). "미리보기로 갈지 판단"을 없애고 상황(파일을 만들 수 있나 / 글자만 쓸 수 있나)이 정하도록 SKILL·README 재작성
- 라이브러리 헤더에 저작권(ScreenSpec)·MIT 표기 — 인라인 배포 시 LICENSE 파일이 따라가지 않으므로. lint가 LICENSE와 동일한지 검사

## v0.13.0 (2026-08-23)
- **미리보기 환경(클로드 아티팩트 등) 대응** — 그런 환경은 바깥 주소로의 요청을 막아 CDN 한 줄이 조용히 실패한다(실측 확인). 대응 둘: `scripts/inline.js`로 라이브러리를 파일 안에 넣어 자체 완결 HTML 생성 / 셸이 없을 때는 실패를 알리는 안내문을 붙이도록 SKILL에 명시
- SKILL §3-C 신설, README 트러블슈팅·개발 절 보강

## v0.12.0 (2026-08-23)
- **설정 없이 스크립트만 붙인 경우 페이지를 감싸지 않는다** — DOM을 그대로 두고 안내 카드 + 콘솔 경고. 첫 사용자가 "망가졌다"고 느끼던 온보딩 실패 수리
- 문서 재구성: README를 판단(이게 나한테 필요한가) + 복붙 가능한 완성 예제 중심으로 재작성, 전체 설정 레퍼런스를 `docs/config.md`로 분리
- 에이전트 진입점 신설: `AGENTS.md`(저장소 수정 규칙) · `llms.txt`(문서 목차)
- 문서 동기화를 기계 검증으로 전환: 코드가 읽는 설정 필드·anno·HTML 속성·공개 API가 레퍼런스에 없으면 lint 실패, README 빠른 시작 예제는 e2e가 실제로 실행

## v0.11.0 (2026-08-22)
- 화면 목록을 트리로 재설계 (Figma 레이어·Notion 사이드바 패턴): 뎁스 = 인덴트 + 세로 가이드선, 중간 경로는 회색 그룹 행, 화면은 이름 + ID 클릭 행

## v0.10.3 (2026-08-22)
- 화면 ID를 드롭다운(select) 모양 칩으로 — 화면 목록이 열린다는 것을 보기 전부터 알 수 있게 (어포던스)

## v0.10.2 (2026-08-22)
- 화살표 규칙 재설계: 기본 = 요소 밖(화면 중심 쪽) 56px에서 요소 가장자리를 가리키는 콜아웃 지시선 — 작은 아이콘·큰 영역 동일 규칙. `arrowTo`로 요소→요소 관계선 지정 가능 (옛 고정 기하 제거)
- MOA 데모: 화살표를 찜 버튼(작은 요소)에 적용, 상품 이미지는 영역으로

## v0.10.1 (2026-08-22)
- overlay 모드 ScreenSpec UI를 브라우저 최대 z 대역으로 — 앱 z-index와 무관하게 항상 맨 위 (범용성)
- MOA 데모 탭바 밀착 실수정 (선택자 우선순위 동점 해소) + e2e 가드

## v0.10.0 (2026-08-22)
- 액센트 컬러 묶음 테마: accent 옵션 (프리셋 5종 + hex) — 마커·하이라이트·버튼·그립·목차가 세트로 변경, lint가 하드코딩 기계 검증
- 하이라이트 재설계: 바깥 확장 0 (안쪽 링 + 연한 채움) — 촘촘한 레이아웃에서 이웃 침범 해소
- 목차 위계 수정: 부모 화면이 자기 이름 섹션 안에 포함, 크럼은 중간 세그먼트만
- wrap 마커 가장자리 클램프 (앱형 여백 0 대응)

## v0.9.2 (2026-08-22)
- 해시 라우터(#/경로) 지원 — 화면 감지가 # 뒤 경로를 읽고 hashchange 추적 (#4 해결)

## v0.9.1 (2026-08-22)
- 목차 행 우측 = 항목 수 대신 화면 ID (길면 앞을 잘라 뒷자리 보존, 전체는 툴팁)
- 설정 진단: ID 중복·깨진 flowTo 콘솔 경고
- SKILL: 기존 ID 체계 존중 룰 명문화 (형식 강요 없음)

## v0.9.0 (2026-08-22)
- 목차 재설계: 커맨드 팔레트 패턴 — 그룹 행 제거, 섹션 라벨 + 행 내 브레드크럼 (그룹 클릭 혼란·닫힘 버그 해소)
- 화면 전환 알림 토스트 ("→ ID · 화면명") — 이동 인지 강화
- flow 버튼 라벨에 대상 화면명 자동 표기

## v0.8.0 (2026-08-22)
- 화면 목록 계층 트리: path 배열 기반 자동 그룹핑(최대 4뎁스, 상수로 6까지 확장 가능) + 그룹 접기
- 모바일 목차 = 전체 화면 시트 (PC = 드롭다운 유지)

## v0.7.0 (2026-08-22)
- 화면 목록(목차): 화면 ID 클릭 → 전체 화면·커버리지(N/M 정의됨)·행 클릭 이동. screens 배열에서 자동 생성
- 패널 라벨 "기능정의" → "기능 설명"
- v0.6.1~3: 모바일 터치 드래그(touch-action)·풀렝스 거터 바·뷰포트 클램프·하단 시트 패널

## v0.6.0 (2026-08-22)
- 내부 구조: 마커·기능정의·활성화·화살표·툴팁을 createCore() 공통 코어로 통합 (wrap/overlay 중복 제거, #2)
- tests/e2e.js 회귀 스위트 내장 — 19케이스, 릴리스 전 필수 (#3)
- 기능정의·헤더·툴팁 텍스트 HTML 이스케이프 (#5)
- prefers-reduced-motion 시 스크롤 애니메이션 비활성 (#6)

## v0.5.x (2026-08-22)
- overlay 모드 신설: React·Next 등 프레임워크용, DOM 불변, 라우트 기반 화면 추적 (v0.5.0)
- 미정의 화면 정직 표시 + 콘솔 자가 진단 (v0.5.1)
- overlay가 호스트 body 배경을 오염시키던 문제 수정 (v0.5.2)
- 이중 로드 가드 · 라우트 prefix 경계 매칭 (v0.5.3)
- suffix 라우트 매칭: Next basePath·정적 호스팅 하위경로 지원 (v0.5.4)

## v0.4.0 (2026-08-22)
- ScreenSpec으로 개명 (구 SpecLayer, 호환 별칭 유지) · 워터마크 미니 배지

## v0.1 ~ v0.3 (2026-08-21)
- 최초 공개: 2모드(프로토타입/화면정의서) · anno 8종 · DevTools식 기기 뷰포트 · 다중 화면 · SKILL.md 하네스
